// File automatically generated by wgsl_bindgen^
//
// ^ wgsl_bindgen version 0.22.2
// Changes made to this file will not be saved.
// SourceHash: 50549a7ef365528f283725727d5d9f46420bb358b376a2bbbf70cfa43c4b3610

#![allow(unused, non_snake_case, non_camel_case_types, non_upper_case_globals)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum ShaderEntry {
    Common,
    RenderShape,
    RenderAabb,
    ResetGridAabb,
    CalculateGridAabb,
    AssignObjectCells,
    CalculateCellOffsetsDispatchDimensions,
    CalculateCellOffsets,
    PopulateGridCells,
    CollisionBroadPhaseGrid,
    CollisionNarrowPhaseDispatchDimensions,
    CollisionNarrowPhase,
    Integrate,
}
impl ShaderEntry {
    pub fn create_pipeline_layout(&self, device: &wgpu::Device) -> wgpu::PipelineLayout {
        match self {
            Self::Common => common::create_pipeline_layout(device),
            Self::RenderShape => render_shape::create_pipeline_layout(device),
            Self::RenderAabb => render_aabb::create_pipeline_layout(device),
            Self::ResetGridAabb => reset_grid_aabb::create_pipeline_layout(device),
            Self::CalculateGridAabb => calculate_grid_aabb::create_pipeline_layout(device),
            Self::AssignObjectCells => assign_object_cells::create_pipeline_layout(device),
            Self::CalculateCellOffsetsDispatchDimensions => {
                calculate_cell_offsets_dispatch_dimensions::create_pipeline_layout(device)
            }
            Self::CalculateCellOffsets => calculate_cell_offsets::create_pipeline_layout(device),
            Self::PopulateGridCells => populate_grid_cells::create_pipeline_layout(device),
            Self::CollisionBroadPhaseGrid => collision_broad_phase_grid::create_pipeline_layout(device),
            Self::CollisionNarrowPhaseDispatchDimensions => {
                collision_narrow_phase_dispatch_dimensions::create_pipeline_layout(device)
            }
            Self::CollisionNarrowPhase => collision_narrow_phase::create_pipeline_layout(device),
            Self::Integrate => integrate::create_pipeline_layout(device),
        }
    }
    pub fn create_shader_module_embed_source(&self, device: &wgpu::Device) -> wgpu::ShaderModule {
        match self {
            Self::Common => common::create_shader_module_embed_source(device),
            Self::RenderShape => render_shape::create_shader_module_embed_source(device),
            Self::RenderAabb => render_aabb::create_shader_module_embed_source(device),
            Self::ResetGridAabb => reset_grid_aabb::create_shader_module_embed_source(device),
            Self::CalculateGridAabb => calculate_grid_aabb::create_shader_module_embed_source(device),
            Self::AssignObjectCells => assign_object_cells::create_shader_module_embed_source(device),
            Self::CalculateCellOffsetsDispatchDimensions => {
                calculate_cell_offsets_dispatch_dimensions::create_shader_module_embed_source(device)
            }
            Self::CalculateCellOffsets => calculate_cell_offsets::create_shader_module_embed_source(device),
            Self::PopulateGridCells => populate_grid_cells::create_shader_module_embed_source(device),
            Self::CollisionBroadPhaseGrid => collision_broad_phase_grid::create_shader_module_embed_source(device),
            Self::CollisionNarrowPhaseDispatchDimensions => {
                collision_narrow_phase_dispatch_dimensions::create_shader_module_embed_source(device)
            }
            Self::CollisionNarrowPhase => collision_narrow_phase::create_shader_module_embed_source(device),
            Self::Integrate => integrate::create_shader_module_embed_source(device),
        }
    }
}
mod _root {
    pub use super::*;
    pub trait SetBindGroup {
        fn set_bind_group(&mut self, index: u32, bind_group: &wgpu::BindGroup, offsets: &[wgpu::DynamicOffset]);
    }
    impl SetBindGroup for wgpu::RenderPass<'_> {
        fn set_bind_group(&mut self, index: u32, bind_group: &wgpu::BindGroup, offsets: &[wgpu::DynamicOffset]) {
            self.set_bind_group(index, bind_group, offsets);
        }
    }
    impl SetBindGroup for wgpu::RenderBundleEncoder<'_> {
        fn set_bind_group(&mut self, index: u32, bind_group: &wgpu::BindGroup, offsets: &[wgpu::DynamicOffset]) {
            self.set_bind_group(index, bind_group, offsets);
        }
    }
    impl SetBindGroup for wgpu::ComputePass<'_> {
        fn set_bind_group(&mut self, index: u32, bind_group: &wgpu::BindGroup, offsets: &[wgpu::DynamicOffset]) {
            self.set_bind_group(index, bind_group, offsets);
        }
    }
}
pub mod layout_asserts {
    use super::{_root, _root::*};
    const WGSL_BASE_TYPE_ASSERTS: () = {};
    const COMMON_CAMERA_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::Camera, inner) == 0);
        assert!(std::mem::size_of::<common::Camera>() == 64);
    };
    const COMMON_FLAGS_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::Flags, inner) == 0);
        assert!(std::mem::size_of::<common::Flags>() == 4);
    };
    const COMMON_VELOCITY_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::Velocity, inner) == 0);
        assert!(std::mem::size_of::<common::Velocity>() == 8);
    };
    const COMMON_MASS_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::Mass, inner) == 0);
        assert!(std::mem::size_of::<common::Mass>() == 4);
    };
    const COMMON_COLOR_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::Color, inner) == 0);
        assert!(std::mem::size_of::<common::Color>() == 16);
    };
    const COMMON_SHAPE_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::Shape, inner) == 0);
        assert!(std::mem::size_of::<common::Shape>() == 4);
    };
    const COMMON_POSITION_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::Position, inner) == 0);
        assert!(std::mem::size_of::<common::Position>() == 8);
    };
    const COMMON_CELL_POSITION_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::CellPosition, cell) == 0);
        assert!(std::mem::offset_of!(common::CellPosition, offset) == 8);
        assert!(std::mem::size_of::<common::CellPosition>() == 16);
    };
    const COMMON_DISPATCH_INDIRECT_ARGS_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::DispatchIndirectArgs, x) == 0);
        assert!(std::mem::offset_of!(common::DispatchIndirectArgs, y) == 4);
        assert!(std::mem::offset_of!(common::DispatchIndirectArgs, z) == 8);
        assert!(std::mem::size_of::<common::DispatchIndirectArgs>() == 12);
    };
    const COMMON_COLLISION_CANDIDATE_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::CollisionCandidate, a) == 0);
        assert!(std::mem::offset_of!(common::CollisionCandidate, b) == 4);
        assert!(std::mem::size_of::<common::CollisionCandidate>() == 8);
    };
    const COMMON_A_A_B_B_ASSERTS: () = {
        assert!(std::mem::offset_of!(common::AABB, min) == 0);
        assert!(std::mem::offset_of!(common::AABB, max) == 8);
        assert!(std::mem::size_of::<common::AABB>() == 16);
    };
    const INTEGRATE_BLACK_HOLE_ASSERTS: () = {
        assert!(std::mem::offset_of!(integrate::BlackHole, position) == 0);
        assert!(std::mem::offset_of!(integrate::BlackHole, radius) == 8);
        assert!(std::mem::offset_of!(integrate::BlackHole, mass) == 12);
        assert!(std::mem::offset_of!(integrate::BlackHole, spin) == 16);
        assert!(std::mem::size_of::<integrate::BlackHole>() == 24);
    };
}
pub mod common {
    use super::{_root, _root::*};
    pub const WORKGROUP_SIZE: u32 = 64u32;
    pub const FLAG_DRAW_OBJECT: u32 = 1u32;
    pub const FLAG_DRAW_AABB: u32 = 2u32;
    pub const FLAG_PHYSICAL: u32 = 4u32;
    pub const FLAG_VELOCITY_COLOR: u32 = 8u32;
    pub const FLAG_COLLISION: u32 = 16u32;
    pub const MAX_CANDIDATES_PER_OBJECT: u32 = 16u32;
    pub const MAX_OBJECTS_PER_CELL: u32 = 2u32;
    pub const MAX_DISPATCH_DIMENSION: u32 = 65535u32;
    pub const INFINITY: f32 = 10000000000f32;
    #[repr(C, align(16))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct Camera {
        #[doc = "offset: 0, size: 64, type: `mat4x4<f32>`"]
        pub inner: [[f32; 4]; 4],
    }
    impl Camera {
        pub const fn new(inner: [[f32; 4]; 4]) -> Self {
            Self { inner }
        }
    }
    #[repr(C, align(4))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct Flags {
        #[doc = "offset: 0, size: 4, type: `u32`"]
        pub inner: u32,
    }
    impl Flags {
        pub const fn new(inner: u32) -> Self {
            Self { inner }
        }
    }
    #[repr(C, align(8))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct Velocity {
        #[doc = "offset: 0, size: 8, type: `vec2<f32>`"]
        pub inner: [f32; 2],
    }
    impl Velocity {
        pub const fn new(inner: [f32; 2]) -> Self {
            Self { inner }
        }
    }
    #[repr(C, align(4))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct Mass {
        #[doc = "offset: 0, size: 4, type: `f32`"]
        pub inner: f32,
    }
    impl Mass {
        pub const fn new(inner: f32) -> Self {
            Self { inner }
        }
    }
    #[repr(C, align(16))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct Color {
        #[doc = "offset: 0, size: 16, type: `vec4<f32>`"]
        pub inner: [f32; 4],
    }
    impl Color {
        pub const fn new(inner: [f32; 4]) -> Self {
            Self { inner }
        }
    }
    #[repr(C, align(4))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct Shape {
        #[doc = "offset: 0, size: 4, type: `u32`"]
        pub inner: u32,
    }
    impl Shape {
        pub const fn new(inner: u32) -> Self {
            Self { inner }
        }
    }
    #[repr(C, align(8))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct Position {
        #[doc = "offset: 0, size: 8, type: `vec2<f32>`"]
        pub inner: [f32; 2],
    }
    impl Position {
        pub const fn new(inner: [f32; 2]) -> Self {
            Self { inner }
        }
    }
    #[repr(C, align(8))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct CellPosition {
        #[doc = "offset: 0, size: 8, type: `vec2<u32>`"]
        pub cell: [u32; 2],
        #[doc = "offset: 8, size: 4, type: `u32`"]
        pub offset: u32,
        pub _pad_offset: [u8; 0x4],
    }
    impl CellPosition {
        pub const fn new(cell: [u32; 2], offset: u32) -> Self {
            Self {
                cell,
                offset,
                _pad_offset: [0; 0x4],
            }
        }
    }
    #[repr(C)]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct CellPositionInit {
        pub cell: [u32; 2],
        pub offset: u32,
    }
    impl CellPositionInit {
        pub fn build(&self) -> CellPosition {
            CellPosition {
                cell: self.cell,
                offset: self.offset,
                _pad_offset: [0; 0x4],
            }
        }
    }
    impl From<CellPositionInit> for CellPosition {
        fn from(data: CellPositionInit) -> Self {
            data.build()
        }
    }
    #[repr(C, align(4))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct DispatchIndirectArgs {
        #[doc = "offset: 0, size: 4, type: `u32`"]
        pub x: u32,
        #[doc = "offset: 4, size: 4, type: `u32`"]
        pub y: u32,
        #[doc = "offset: 8, size: 4, type: `u32`"]
        pub z: u32,
    }
    impl DispatchIndirectArgs {
        pub const fn new(x: u32, y: u32, z: u32) -> Self {
            Self { x, y, z }
        }
    }
    #[repr(C, align(4))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct CollisionCandidate {
        #[doc = "offset: 0, size: 4, type: `u32`"]
        pub a: u32,
        #[doc = "offset: 4, size: 4, type: `u32`"]
        pub b: u32,
    }
    impl CollisionCandidate {
        pub const fn new(a: u32, b: u32) -> Self {
            Self { a, b }
        }
    }
    #[repr(C, align(8))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct AABB {
        #[doc = "offset: 0, size: 8, type: `vec2<f32>`"]
        pub min: [f32; 2],
        #[doc = "offset: 8, size: 8, type: `vec2<f32>`"]
        pub max: [f32; 2],
    }
    impl AABB {
        pub const fn new(min: [f32; 2], max: [f32; 2]) -> Self {
            Self { min, max }
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 0]) -> [wgpu::BindGroupLayout; 0] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("Common::PipelineLayout"),
            bind_group_layouts: &[],
            immediate_size: 0u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("common.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct Camera {
    inner: mat4x4<f32>,
}

struct Flags {
    inner: u32,
}

struct Velocity {
    inner: vec2<f32>,
}

struct Mass {
    inner: f32,
}

struct Color {
    inner: vec4<f32>,
}

struct Shape {
    inner: u32,
}

struct Position {
    inner: vec2<f32>,
}

struct Radius {
    inner: f32,
}

struct AABB {
    min: vec2<f32>,
    max: vec2<f32>,
}

struct DispatchIndirectArgs {
    x: u32,
    y: u32,
    z: u32,
}

struct CollisionCandidate {
    a: u32,
    b: u32,
}

struct CellPosition {
    cell: vec2<u32>,
    offset: u32,
}

const WORKGROUP_SIZE: u32 = 64u;
const UNIT_QUAD_VERTICES: array<vec2<f32>, 6> = array<vec2<f32>, 6>(vec2<f32>(0.5f, 0.5f), vec2<f32>(-0.5f, 0.5f), vec2<f32>(-0.5f, -0.5f), vec2<f32>(-0.5f, -0.5f), vec2<f32>(0.5f, -0.5f), vec2<f32>(0.5f, 0.5f));
const FLAG_DRAW_OBJECT: u32 = 1u;
const FLAG_DRAW_AABB: u32 = 2u;
const FLAG_PHYSICAL: u32 = 4u;
const FLAG_VELOCITY_COLOR: u32 = 8u;
const FLAG_COLLISION: u32 = 16u;
const MAX_CANDIDATES_PER_OBJECT: u32 = 16u;
const MAX_OBJECTS_PER_CELL: u32 = 2u;
const MAX_DISPATCH_DIMENSION: u32 = 65535u;
const INFINITY: f32 = 10000000000f;

fn flat_invocation_index(gid: vec3<u32>, nwg: vec3<u32>, workgroup_size: u32) -> u32 {
    return ((gid.x + ((gid.y * workgroup_size) * nwg.x)) + ((((gid.z * workgroup_size) * nwg.x) * workgroup_size) * nwg.y));
}

fn div_ceil(a: u32, b: u32) -> u32 {
    return (((a + b) - 1u) / b);
}

"#;
}
pub mod bytemuck_impls {
    use super::{_root, _root::*};
    unsafe impl bytemuck::Zeroable for common::Camera {}
    unsafe impl bytemuck::Pod for common::Camera {}
    unsafe impl bytemuck::Zeroable for common::Flags {}
    unsafe impl bytemuck::Pod for common::Flags {}
    unsafe impl bytemuck::Zeroable for common::Velocity {}
    unsafe impl bytemuck::Pod for common::Velocity {}
    unsafe impl bytemuck::Zeroable for common::Mass {}
    unsafe impl bytemuck::Pod for common::Mass {}
    unsafe impl bytemuck::Zeroable for common::Color {}
    unsafe impl bytemuck::Pod for common::Color {}
    unsafe impl bytemuck::Zeroable for common::Shape {}
    unsafe impl bytemuck::Pod for common::Shape {}
    unsafe impl bytemuck::Zeroable for common::Position {}
    unsafe impl bytemuck::Pod for common::Position {}
    unsafe impl bytemuck::Zeroable for common::CellPosition {}
    unsafe impl bytemuck::Pod for common::CellPosition {}
    unsafe impl bytemuck::Zeroable for common::DispatchIndirectArgs {}
    unsafe impl bytemuck::Pod for common::DispatchIndirectArgs {}
    unsafe impl bytemuck::Zeroable for common::CollisionCandidate {}
    unsafe impl bytemuck::Pod for common::CollisionCandidate {}
    unsafe impl bytemuck::Zeroable for common::AABB {}
    unsafe impl bytemuck::Pod for common::AABB {}
    unsafe impl bytemuck::Zeroable for integrate::BlackHole {}
    unsafe impl bytemuck::Pod for integrate::BlackHole {}
}
pub mod render_shape {
    use super::{_root, _root::*};
    pub const SHAPE_RECT: u32 = 0u32;
    pub const SHAPE_CIRCLE: u32 = 1u32;
    pub const ENTRY_VS_MAIN: &str = "vs_main";
    pub const ENTRY_FS_MAIN: &str = "fs_main";
    #[derive(Debug)]
    pub struct VertexEntry<const N: usize> {
        pub entry_point: &'static str,
        pub buffers: [Option<wgpu::VertexBufferLayout<'static>>; N],
        pub constants: Vec<(&'static str, f64)>,
    }
    pub fn vertex_state<'a, const N: usize>(
        module: &'a wgpu::ShaderModule,
        entry: &'a VertexEntry<N>,
    ) -> wgpu::VertexState<'a> {
        wgpu::VertexState {
            module,
            entry_point: Some(entry.entry_point),
            buffers: &entry.buffers,
            compilation_options: wgpu::PipelineCompilationOptions {
                constants: &entry.constants,
                ..Default::default()
            },
        }
    }
    pub fn vs_main_entry() -> VertexEntry<0> {
        VertexEntry {
            entry_point: ENTRY_VS_MAIN,
            buffers: [],
            constants: Default::default(),
        }
    }
    #[derive(Debug)]
    pub struct FragmentEntry<const N: usize> {
        pub entry_point: &'static str,
        pub targets: [Option<wgpu::ColorTargetState>; N],
        pub constants: Vec<(&'static str, f64)>,
    }
    pub fn fragment_state<'a, const N: usize>(
        module: &'a wgpu::ShaderModule,
        entry: &'a FragmentEntry<N>,
    ) -> wgpu::FragmentState<'a> {
        wgpu::FragmentState {
            module,
            entry_point: Some(entry.entry_point),
            targets: &entry.targets,
            compilation_options: wgpu::PipelineCompilationOptions {
                constants: &entry.constants,
                ..Default::default()
            },
        }
    }
    pub fn fs_main_entry(targets: [Option<wgpu::ColorTargetState>; 1]) -> FragmentEntry<1> {
        FragmentEntry {
            entry_point: ENTRY_FS_MAIN,
            targets,
            constants: Default::default(),
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub camera: wgpu::BufferBinding<'a>,
        pub energy_spectrum_width: wgpu::BufferBinding<'a>,
        pub particle_radius: wgpu::BufferBinding<'a>,
        pub colors: wgpu::BufferBinding<'a>,
        pub shapes: wgpu::BufferBinding<'a>,
        pub masses: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub camera: wgpu::BindGroupEntry<'a>,
        pub energy_spectrum_width: wgpu::BindGroupEntry<'a>,
        pub particle_radius: wgpu::BindGroupEntry<'a>,
        pub colors: wgpu::BindGroupEntry<'a>,
        pub shapes: wgpu::BindGroupEntry<'a>,
        pub masses: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                camera: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.camera),
                },
                energy_spectrum_width: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.energy_spectrum_width),
                },
                particle_radius: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
                colors: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.colors),
                },
                shapes: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.shapes),
                },
                masses: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.masses),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 6] {
            [
                self.camera,
                self.energy_spectrum_width,
                self.particle_radius,
                self.colors,
                self.shapes,
                self.masses,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("RenderShape::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"camera\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<_root::common::Camera>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"energy_spectrum_width\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"colors\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"shapes\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"masses\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("RenderShape::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1EntriesParams<'a> {
        pub flags: wgpu::BufferBinding<'a>,
        pub positions: wgpu::BufferBinding<'a>,
        pub velocities: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup1Entries<'a> {
        pub flags: wgpu::BindGroupEntry<'a>,
        pub positions: wgpu::BindGroupEntry<'a>,
        pub velocities: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup1Entries<'a> {
        pub fn new(params: WgpuBindGroup1EntriesParams<'a>) -> Self {
            Self {
                flags: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.flags),
                },
                positions: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.positions),
                },
                velocities: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.velocities),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 3] {
            [self.flags, self.positions, self.velocities]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1(wgpu::BindGroup);
    impl WgpuBindGroup1 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("RenderShape::BindGroup1::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"flags\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"velocities\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup1Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("RenderShape::BindGroup1"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(1, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
        pub bind_group1: &'a WgpuBindGroup1,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
            self.bind_group1.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 2]) -> [wgpu::BindGroupLayout; 2] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("RenderShape::PipelineLayout"),
            bind_group_layouts: &[
                Some(&WgpuBindGroup0::get_bind_group_layout(device)),
                Some(&WgpuBindGroup1::get_bind_group_layout(device)),
            ],
            immediate_size: 0u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("render_shape.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct CameraX_naga_oil_mod_XMNXW23LPNYX {
    inner: mat4x4<f32>,
}

struct FlagsX_naga_oil_mod_XMNXW23LPNYX {
    inner: u32,
}

struct VelocityX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct MassX_naga_oil_mod_XMNXW23LPNYX {
    inner: f32,
}

struct ColorX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec4<f32>,
}

struct ShapeX_naga_oil_mod_XMNXW23LPNYX {
    inner: u32,
}

struct PositionX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct VertexOutput {
    @builtin(position) clip_position: vec4<f32>,
    @location(0) quad_position: vec2<f32>,
    @location(1) color: vec4<f32>,
    @location(2) @interpolate(flat) shape: u32,
}

const UNIT_QUAD_VERTICESX_naga_oil_mod_XMNXW23LPNYX: array<vec2<f32>, 6> = array<vec2<f32>, 6>(vec2<f32>(0.5f, 0.5f), vec2<f32>(-0.5f, 0.5f), vec2<f32>(-0.5f, -0.5f), vec2<f32>(-0.5f, -0.5f), vec2<f32>(0.5f, -0.5f), vec2<f32>(0.5f, 0.5f));
const FLAG_DRAW_OBJECTX_naga_oil_mod_XMNXW23LPNYX: u32 = 1u;
const FLAG_VELOCITY_COLORX_naga_oil_mod_XMNXW23LPNYX: u32 = 8u;
const SHAPE_RECT: u32 = 0u;
const SHAPE_CIRCLE: u32 = 1u;

@group(0) @binding(0) 
var<uniform> camera: CameraX_naga_oil_mod_XMNXW23LPNYX;
@group(0) @binding(1) 
var<uniform> energy_spectrum_width: f32;
@group(0) @binding(2) 
var<uniform> particle_radius: f32;
@group(0) @binding(3) 
var<storage> colors: array<ColorX_naga_oil_mod_XMNXW23LPNYX>;
@group(0) @binding(4) 
var<storage> shapes: array<ShapeX_naga_oil_mod_XMNXW23LPNYX>;
@group(0) @binding(5) 
var<storage> masses: array<MassX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(0) 
var<storage> flags: array<FlagsX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(1) 
var<storage> positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(2) 
var<storage> velocities: array<VelocityX_naga_oil_mod_XMNXW23LPNYX>;

fn wavelength_to_rgb(lambda: f32) -> vec3<f32> {
    var r: f32 = 0f;
    var g: f32 = 0f;
    var b: f32 = 0f;
    var local: bool;

    if (lambda >= 380f) {
        local = (lambda < 440f);
    } else {
        local = false;
    }
    let _e9 = local;
    if _e9 {
        r = (-((lambda - 440f)) / 60f);
        b = 1f;
    } else {
        if (lambda < 490f) {
            g = ((lambda - 440f) / 50f);
            b = 1f;
        } else {
            if (lambda < 510f) {
                g = 1f;
                b = (-((lambda - 510f)) / 20f);
            } else {
                if (lambda < 580f) {
                    r = ((lambda - 510f) / 70f);
                    g = 1f;
                } else {
                    if (lambda < 645f) {
                        r = 1f;
                        g = (-((lambda - 645f)) / 65f);
                    } else {
                        if (lambda <= 700f) {
                            r = 1f;
                        }
                    }
                }
            }
        }
    }
    let _e52 = r;
    let _e53 = g;
    let _e54 = b;
    return vec3<f32>(_e52, _e53, _e54);
}

fn spectral_intensity(lambda_1: f32) -> f32 {
    if (lambda_1 < 420f) {
        return (0.3f + ((0.7f * (lambda_1 - 380f)) / 40f));
    }
    if (lambda_1 > 645f) {
        return (0.3f + ((0.7f * (700f - lambda_1)) / 55f));
    }
    return 1f;
}

fn spectral_color(spectral_position: f32) -> vec4<f32> {
    let lambda_2 = mix(700f, 380f, spectral_position);
    let _e4 = wavelength_to_rgb(lambda_2);
    let _e5 = spectral_intensity(lambda_2);
    let intensity = max(0.2f, _e5);
    return vec4<f32>((_e4 * intensity), 0.1f);
}

fn sdf_cirle(p: vec2<f32>) -> f32 {
    return (length(p) - 0.5f);
}

@vertex 
fn vs_main(@builtin(vertex_index) vertex_index: u32, @builtin(instance_index) i: u32) -> VertexOutput {
    var out: VertexOutput = VertexOutput();
    var velocity: vec2<f32>;
    var scale: f32;

    let f = flags[i].inner;
    if ((f & FLAG_DRAW_OBJECTX_naga_oil_mod_XMNXW23LPNYX) == 0u) {
        let _e11 = out;
        return _e11;
    }
    if ((f & FLAG_VELOCITY_COLORX_naga_oil_mod_XMNXW23LPNYX) != 0u) {
        let _e19 = velocities[i].inner;
        velocity = _e19;
        let mass = masses[i].inner;
        let _e25 = velocity;
        let _e33 = energy_spectrum_width;
        let spectral_position_1 = saturate((((mass * pow(length(_e25), 2f)) / 2f) / pow(_e33, 2f)));
        let _e40 = spectral_color(sqrt(spectral_position_1));
        out.color = _e40;
    } else {
        let _e45 = colors[i].inner;
        out.color = _e45;
    }
    let _e47 = particle_radius;
    scale = (_e47 * 2f);
    let position = positions[i].inner;
    let _e55 = scale;
    let _e56 = scale;
    let model = mat4x4<f32>(vec4<f32>(_e55, 0f, 0f, 0f), vec4<f32>(0f, _e56, 0f, 0f), vec4<f32>(0f, 0f, 1f, 0f), vec4<f32>(position.x, position.y, 0f, 1f));
    let vertex = UNIT_QUAD_VERTICESX_naga_oil_mod_XMNXW23LPNYX[vertex_index];
    let _e82 = camera.inner;
    out.clip_position = ((_e82 * model) * vec4<f32>(vertex, 0f, 1f));
    out.quad_position = vertex;
    let _e93 = shapes[i].inner;
    out.shape = _e93;
    let _e94 = out;
    return _e94;
}

@fragment 
fn fs_main(in: VertexOutput) -> @location(0) vec4<f32> {
    var color: vec4<f32>;

    color = in.color;
    let _e4 = sdf_cirle(in.quad_position);
    let _e5 = fwidth(_e4);
    let w = (_e5 / 2f);
    if (in.shape == SHAPE_CIRCLE) {
        let _e12 = color.w;
        color.w = (_e12 * smoothstep(w, -(w), _e4));
    }
    let _e16 = color;
    return _e16;
}
"#;
}
pub mod render_aabb {
    use super::{_root, _root::*};
    pub const ENTRY_VS_MAIN: &str = "vs_main";
    pub const ENTRY_FS_MAIN: &str = "fs_main";
    #[derive(Debug)]
    pub struct VertexEntry<const N: usize> {
        pub entry_point: &'static str,
        pub buffers: [Option<wgpu::VertexBufferLayout<'static>>; N],
        pub constants: Vec<(&'static str, f64)>,
    }
    pub fn vertex_state<'a, const N: usize>(
        module: &'a wgpu::ShaderModule,
        entry: &'a VertexEntry<N>,
    ) -> wgpu::VertexState<'a> {
        wgpu::VertexState {
            module,
            entry_point: Some(entry.entry_point),
            buffers: &entry.buffers,
            compilation_options: wgpu::PipelineCompilationOptions {
                constants: &entry.constants,
                ..Default::default()
            },
        }
    }
    pub fn vs_main_entry() -> VertexEntry<0> {
        VertexEntry {
            entry_point: ENTRY_VS_MAIN,
            buffers: [],
            constants: Default::default(),
        }
    }
    #[derive(Debug)]
    pub struct FragmentEntry<const N: usize> {
        pub entry_point: &'static str,
        pub targets: [Option<wgpu::ColorTargetState>; N],
        pub constants: Vec<(&'static str, f64)>,
    }
    pub fn fragment_state<'a, const N: usize>(
        module: &'a wgpu::ShaderModule,
        entry: &'a FragmentEntry<N>,
    ) -> wgpu::FragmentState<'a> {
        wgpu::FragmentState {
            module,
            entry_point: Some(entry.entry_point),
            targets: &entry.targets,
            compilation_options: wgpu::PipelineCompilationOptions {
                constants: &entry.constants,
                ..Default::default()
            },
        }
    }
    pub fn fs_main_entry(targets: [Option<wgpu::ColorTargetState>; 1]) -> FragmentEntry<1> {
        FragmentEntry {
            entry_point: ENTRY_FS_MAIN,
            targets,
            constants: Default::default(),
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub camera: wgpu::BufferBinding<'a>,
        pub particle_radius: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub camera: wgpu::BindGroupEntry<'a>,
        pub particle_radius: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                camera: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.camera),
                },
                particle_radius: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 2] {
            [self.camera, self.particle_radius]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("RenderAabb::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"camera\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<_root::common::Camera>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("RenderAabb::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1EntriesParams<'a> {
        pub flags: wgpu::BufferBinding<'a>,
        pub positions: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup1Entries<'a> {
        pub flags: wgpu::BindGroupEntry<'a>,
        pub positions: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup1Entries<'a> {
        pub fn new(params: WgpuBindGroup1EntriesParams<'a>) -> Self {
            Self {
                flags: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.flags),
                },
                positions: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.positions),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 2] {
            [self.flags, self.positions]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1(wgpu::BindGroup);
    impl WgpuBindGroup1 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("RenderAabb::BindGroup1::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(2): \"flags\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::VERTEX_FRAGMENT,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup1Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("RenderAabb::BindGroup1"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(1, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
        pub bind_group1: &'a WgpuBindGroup1,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
            self.bind_group1.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 2]) -> [wgpu::BindGroupLayout; 2] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("RenderAabb::PipelineLayout"),
            bind_group_layouts: &[
                Some(&WgpuBindGroup0::get_bind_group_layout(device)),
                Some(&WgpuBindGroup1::get_bind_group_layout(device)),
            ],
            immediate_size: 0u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("render_aabb.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct CameraX_naga_oil_mod_XMNXW23LPNYX {
    inner: mat4x4<f32>,
}

struct FlagsX_naga_oil_mod_XMNXW23LPNYX {
    inner: u32,
}

struct PositionX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct VertexOutput {
    @builtin(position) clip_position: vec4<f32>,
    @location(0) @interpolate(flat) flags: u32,
    @location(1) scale: f32,
    @location(2) quad_position: vec2<f32>,
}

struct FragmentOutput {
    @location(0) color: vec4<f32>,
}

const FLAG_DRAW_AABBX_naga_oil_mod_XMNXW23LPNYX: u32 = 2u;
const UNIT_QUAD_VERTICES: array<vec2<f32>, 5> = array<vec2<f32>, 5>(vec2<f32>(0.5f, 0.5f), vec2<f32>(-0.5f, 0.5f), vec2<f32>(-0.5f, -0.5f), vec2<f32>(0.5f, -0.5f), vec2<f32>(0.5f, 0.5f));

@group(0) @binding(0) 
var<uniform> camera: CameraX_naga_oil_mod_XMNXW23LPNYX;
@group(0) @binding(1) 
var<uniform> particle_radius: f32;
@group(1) @binding(2) 
var<storage> flags_1: array<FlagsX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(3) 
var<storage> positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;

@vertex 
fn vs_main(@builtin(vertex_index) vertex_index: u32, @builtin(instance_index) i: u32) -> VertexOutput {
    var out: VertexOutput = VertexOutput();
    var flags: u32 = FLAG_DRAW_AABBX_naga_oil_mod_XMNXW23LPNYX;

    if (i < arrayLength((&flags_1))) {
        let _e9 = flags_1[i].inner;
        flags = _e9;
        let _e11 = flags;
        if ((_e11 & FLAG_DRAW_AABBX_naga_oil_mod_XMNXW23LPNYX) == 0u) {
            let _e17 = out;
            return _e17;
        }
    }
    let _e19 = particle_radius;
    let scale = (_e19 * 2f);
    let position = positions[i].inner;
    let model = mat4x4<f32>(vec4<f32>(scale, 0f, 0f, 0f), vec4<f32>(0f, scale, 0f, 0f), vec4<f32>(0f, 0f, 1f, 0f), vec4<f32>(position.x, position.y, 0f, 1f));
    let vertex = UNIT_QUAD_VERTICES[vertex_index];
    let _e51 = camera.inner;
    out.clip_position = ((_e51 * model) * vec4<f32>(vertex, 0f, 1f));
    let _e58 = flags;
    out.flags = _e58;
    out.scale = scale;
    out.quad_position = vertex;
    let _e61 = out;
    return _e61;
}

@fragment 
fn fs_main(in: VertexOutput) -> FragmentOutput {
    if ((in.flags & FLAG_DRAW_AABBX_naga_oil_mod_XMNXW23LPNYX) == 0u) {
        discard;
    }
    return FragmentOutput(vec4<f32>(1f, 1f, 1f, 1f));
}
"#;
}
pub mod reset_grid_aabb {
    use super::{_root, _root::*};
    pub mod compute {
        use super::{_root, _root::*};
        pub const RESET_GRID_AABB_WORKGROUP_SIZE: [u32; 3] = [1, 1, 1];
        pub fn create_reset_grid_aabb_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline reset_grid_aabb"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("reset_grid_aabb"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_RESET_GRID_AABB: &str = "reset_grid_aabb";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub grid_min_x: wgpu::BufferBinding<'a>,
        pub grid_max_x: wgpu::BufferBinding<'a>,
        pub grid_min_y: wgpu::BufferBinding<'a>,
        pub grid_max_y: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub grid_min_x: wgpu::BindGroupEntry<'a>,
        pub grid_max_x: wgpu::BindGroupEntry<'a>,
        pub grid_min_y: wgpu::BindGroupEntry<'a>,
        pub grid_max_y: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                grid_min_x: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_x),
                },
                grid_max_x: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.grid_max_x),
                },
                grid_min_y: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_y),
                },
                grid_max_y: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.grid_max_y),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 4] {
            [self.grid_min_x, self.grid_max_x, self.grid_min_y, self.grid_max_y]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("ResetGridAabb::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"grid_min_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"grid_max_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"grid_min_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"grid_max_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("ResetGridAabb::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 1]) -> [wgpu::BindGroupLayout; 1] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("ResetGridAabb::PipelineLayout"),
            bind_group_layouts: &[Some(&WgpuBindGroup0::get_bind_group_layout(device))],
            immediate_size: 0u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("reset_grid_aabb.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
const INFINITYX_naga_oil_mod_XMNXW23LPNYX: f32 = 10000000000f;

@group(0) @binding(0) 
var<storage, read_write> grid_min_x: f32;
@group(0) @binding(1) 
var<storage, read_write> grid_max_x: f32;
@group(0) @binding(2) 
var<storage, read_write> grid_min_y: f32;
@group(0) @binding(3) 
var<storage, read_write> grid_max_y: f32;

@compute @workgroup_size(1, 1, 1) 
fn reset_grid_aabb() {
    grid_min_x = INFINITYX_naga_oil_mod_XMNXW23LPNYX;
    grid_max_x = 0f;
    grid_min_y = INFINITYX_naga_oil_mod_XMNXW23LPNYX;
    grid_max_y = 0f;
    return;
}
"#;
}
pub mod calculate_grid_aabb {
    use super::{_root, _root::*};
    pub mod compute {
        use super::{_root, _root::*};
        pub const CALCULATE_GRID_AABB_WORKGROUP_SIZE: [u32; 3] = [64, 1, 1];
        pub fn create_calculate_grid_aabb_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline calculate_grid_aabb"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("calculate_grid_aabb"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_CALCULATE_GRID_AABB: &str = "calculate_grid_aabb";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub object_count: wgpu::BufferBinding<'a>,
        pub particle_radius: wgpu::BufferBinding<'a>,
        pub grid_min_x: wgpu::BufferBinding<'a>,
        pub grid_max_x: wgpu::BufferBinding<'a>,
        pub grid_min_y: wgpu::BufferBinding<'a>,
        pub grid_max_y: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub object_count: wgpu::BindGroupEntry<'a>,
        pub particle_radius: wgpu::BindGroupEntry<'a>,
        pub grid_min_x: wgpu::BindGroupEntry<'a>,
        pub grid_max_x: wgpu::BindGroupEntry<'a>,
        pub grid_min_y: wgpu::BindGroupEntry<'a>,
        pub grid_max_y: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                object_count: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.object_count),
                },
                particle_radius: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
                grid_min_x: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_x),
                },
                grid_max_x: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.grid_max_x),
                },
                grid_min_y: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_y),
                },
                grid_max_y: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.grid_max_y),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 6] {
            [
                self.object_count,
                self.particle_radius,
                self.grid_min_x,
                self.grid_max_x,
                self.grid_min_y,
                self.grid_max_y,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CalculateGridAabb::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"grid_min_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"grid_max_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"grid_min_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"grid_max_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CalculateGridAabb::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1EntriesParams<'a> {
        pub positions: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup1Entries<'a> {
        pub positions: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup1Entries<'a> {
        pub fn new(params: WgpuBindGroup1EntriesParams<'a>) -> Self {
            Self {
                positions: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.positions),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 1] {
            [self.positions]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1(wgpu::BindGroup);
    impl WgpuBindGroup1 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CalculateGridAabb::BindGroup1::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup1Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CalculateGridAabb::BindGroup1"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(1, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
        pub bind_group1: &'a WgpuBindGroup1,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
            self.bind_group1.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 2]) -> [wgpu::BindGroupLayout; 2] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("CalculateGridAabb::PipelineLayout"),
            bind_group_layouts: &[
                Some(&WgpuBindGroup0::get_bind_group_layout(device)),
                Some(&WgpuBindGroup1::get_bind_group_layout(device)),
            ],
            immediate_size: 4u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("calculate_grid_aabb.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct PositionX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;

var<immediate> thread_offset: u32;
@group(0) @binding(0) 
var<uniform> object_count: u32;
@group(0) @binding(1) 
var<uniform> particle_radius: f32;
@group(0) @binding(2) 
var<storage, read_write> grid_min_x: atomic<u32>;
@group(0) @binding(3) 
var<storage, read_write> grid_max_x: atomic<u32>;
@group(0) @binding(4) 
var<storage, read_write> grid_min_y: atomic<u32>;
@group(0) @binding(5) 
var<storage, read_write> grid_max_y: atomic<u32>;
@group(1) @binding(0) 
var<storage> positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;

@compute @workgroup_size(64, 1, 1) 
fn calculate_grid_aabb(@builtin(global_invocation_id) gid: vec3<u32>, @builtin(subgroup_invocation_id) sid: u32) {
    let _e3 = thread_offset;
    let i = (gid.x + _e3);
    let _e6 = object_count;
    let is_valid = (i < _e6);
    let object_index = select(0u, i, is_valid);
    let position = positions[object_index].inner;
    let _e15 = particle_radius;
    let object_min = (position - vec2(_e15));
    let _e19 = particle_radius;
    let object_max = (position + vec2(_e19));
    let _e23 = subgroupMin(object_min.x);
    let _e25 = subgroupMax(object_max.x);
    let _e27 = subgroupMin(object_min.y);
    let _e29 = subgroupMax(object_max.y);
    if (sid == 0u) {
        let _e35 = atomicMin((&grid_min_x), bitcast<u32>(_e23));
        let _e38 = atomicMax((&grid_max_x), bitcast<u32>(_e25));
        let _e41 = atomicMin((&grid_min_y), bitcast<u32>(_e27));
        let _e44 = atomicMax((&grid_max_y), bitcast<u32>(_e29));
        return;
    } else {
        return;
    }
}
"#;
}
pub mod assign_object_cells {
    use super::{_root, _root::*};
    pub mod compute {
        use super::{_root, _root::*};
        pub const ASSIGN_OBJECT_CELLS_WORKGROUP_SIZE: [u32; 3] = [64, 1, 1];
        pub fn create_assign_object_cells_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline assign_object_cells"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("assign_object_cells"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_ASSIGN_OBJECT_CELLS: &str = "assign_object_cells";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub object_count: wgpu::BufferBinding<'a>,
        pub particle_radius: wgpu::BufferBinding<'a>,
        pub grid_min_x: wgpu::BufferBinding<'a>,
        pub grid_min_y: wgpu::BufferBinding<'a>,
        pub grid_size_x: wgpu::BufferBinding<'a>,
        pub grid_size_y: wgpu::BufferBinding<'a>,
        pub cell_object_count: wgpu::BufferBinding<'a>,
        pub object_cells: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub object_count: wgpu::BindGroupEntry<'a>,
        pub particle_radius: wgpu::BindGroupEntry<'a>,
        pub grid_min_x: wgpu::BindGroupEntry<'a>,
        pub grid_min_y: wgpu::BindGroupEntry<'a>,
        pub grid_size_x: wgpu::BindGroupEntry<'a>,
        pub grid_size_y: wgpu::BindGroupEntry<'a>,
        pub cell_object_count: wgpu::BindGroupEntry<'a>,
        pub object_cells: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                object_count: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.object_count),
                },
                particle_radius: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
                grid_min_x: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_x),
                },
                grid_min_y: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_y),
                },
                grid_size_x: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_x),
                },
                grid_size_y: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_y),
                },
                cell_object_count: wgpu::BindGroupEntry {
                    binding: 6,
                    resource: wgpu::BindingResource::Buffer(params.cell_object_count),
                },
                object_cells: wgpu::BindGroupEntry {
                    binding: 7,
                    resource: wgpu::BindingResource::Buffer(params.object_cells),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 8] {
            [
                self.object_count,
                self.particle_radius,
                self.grid_min_x,
                self.grid_min_y,
                self.grid_size_x,
                self.grid_size_y,
                self.cell_object_count,
                self.object_cells,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("AssignObjectCells::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"grid_min_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"grid_min_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"grid_size_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"grid_size_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(6): \"cell_object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 6,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(7): \"object_cells\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 7,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("AssignObjectCells::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1EntriesParams<'a> {
        pub positions: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup1Entries<'a> {
        pub positions: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup1Entries<'a> {
        pub fn new(params: WgpuBindGroup1EntriesParams<'a>) -> Self {
            Self {
                positions: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.positions),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 1] {
            [self.positions]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1(wgpu::BindGroup);
    impl WgpuBindGroup1 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("AssignObjectCells::BindGroup1::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup1Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("AssignObjectCells::BindGroup1"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(1, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
        pub bind_group1: &'a WgpuBindGroup1,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
            self.bind_group1.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 2]) -> [wgpu::BindGroupLayout; 2] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("AssignObjectCells::PipelineLayout"),
            bind_group_layouts: &[
                Some(&WgpuBindGroup0::get_bind_group_layout(device)),
                Some(&WgpuBindGroup1::get_bind_group_layout(device)),
            ],
            immediate_size: 4u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("assign_object_cells.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct PositionX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct CellPositionX_naga_oil_mod_XMNXW23LPNYX {
    cell: vec2<u32>,
    offset: u32,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;

var<immediate> thread_offset: u32;
@group(0) @binding(0) 
var<uniform> object_count: u32;
@group(0) @binding(1) 
var<uniform> particle_radius: f32;
@group(0) @binding(2) 
var<uniform> grid_min_x: f32;
@group(0) @binding(3) 
var<uniform> grid_min_y: f32;
@group(0) @binding(4) 
var<uniform> grid_size_x: u32;
@group(0) @binding(5) 
var<uniform> grid_size_y: u32;
@group(0) @binding(6) 
var<storage, read_write> cell_object_count: array<atomic<u32>>;
@group(0) @binding(7) 
var<storage, read_write> object_cells: array<CellPositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(0) 
var<storage> positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;

@compute @workgroup_size(64, 1, 1) 
fn assign_object_cells(@builtin(global_invocation_id) gid: vec3<u32>) {
    let _e3 = thread_offset;
    let object_index = (gid.x + _e3);
    let _e6 = object_count;
    if (object_index >= _e6) {
        return;
    }
    let position = positions[object_index].inner;
    let _e13 = particle_radius;
    let cell_size = (_e13 * 2f);
    let _e17 = grid_min_x;
    let _e19 = grid_min_y;
    let _e28 = grid_size_x;
    let _e32 = grid_size_y;
    let cell = clamp(vec2<u32>(((position - vec2<f32>(_e17, _e19)) / vec2(cell_size))), vec2(0u), vec2<u32>((_e28 - 1u), (_e32 - 1u)));
    let _e40 = grid_size_x;
    let cell_index = (cell.x + (cell.y * _e40));
    let _e46 = atomicAdd((&cell_object_count[cell_index]), 1u);
    object_cells[object_index] = CellPositionX_naga_oil_mod_XMNXW23LPNYX(cell, _e46);
    return;
}
"#;
}
pub mod calculate_cell_offsets_dispatch_dimensions {
    use super::{_root, _root::*};
    pub const N_CELL_INDIRECT_DISPATCHES: u32 = 3u32;
    pub mod compute {
        use super::{_root, _root::*};
        pub const CALCULATE_CELL_OFFSETS_DISPATCH_DIMENSIONS_WORKGROUP_SIZE: [u32; 3] = [1, 1, 1];
        pub fn create_calculate_cell_offsets_dispatch_dimensions_pipeline_embed_source(
            device: &wgpu::Device,
        ) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline calculate_cell_offsets_dispatch_dimensions"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("calculate_cell_offsets_dispatch_dimensions"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_CALCULATE_CELL_OFFSETS_DISPATCH_DIMENSIONS: &str = "calculate_cell_offsets_dispatch_dimensions";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub particle_radius: wgpu::BufferBinding<'a>,
        pub grid_min_x: wgpu::BufferBinding<'a>,
        pub grid_max_x: wgpu::BufferBinding<'a>,
        pub grid_min_y: wgpu::BufferBinding<'a>,
        pub grid_max_y: wgpu::BufferBinding<'a>,
        pub grid_size_x: wgpu::BufferBinding<'a>,
        pub grid_size_y: wgpu::BufferBinding<'a>,
        pub cell_offsets_dispatch_dimensions: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub particle_radius: wgpu::BindGroupEntry<'a>,
        pub grid_min_x: wgpu::BindGroupEntry<'a>,
        pub grid_max_x: wgpu::BindGroupEntry<'a>,
        pub grid_min_y: wgpu::BindGroupEntry<'a>,
        pub grid_max_y: wgpu::BindGroupEntry<'a>,
        pub grid_size_x: wgpu::BindGroupEntry<'a>,
        pub grid_size_y: wgpu::BindGroupEntry<'a>,
        pub cell_offsets_dispatch_dimensions: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                particle_radius: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
                grid_min_x: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_x),
                },
                grid_max_x: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.grid_max_x),
                },
                grid_min_y: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_y),
                },
                grid_max_y: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.grid_max_y),
                },
                grid_size_x: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_x),
                },
                grid_size_y: wgpu::BindGroupEntry {
                    binding: 6,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_y),
                },
                cell_offsets_dispatch_dimensions: wgpu::BindGroupEntry {
                    binding: 7,
                    resource: wgpu::BindingResource::Buffer(params.cell_offsets_dispatch_dimensions),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 8] {
            [
                self.particle_radius,
                self.grid_min_x,
                self.grid_max_x,
                self.grid_min_y,
                self.grid_max_y,
                self.grid_size_x,
                self.grid_size_y,
                self.cell_offsets_dispatch_dimensions,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CalculateCellOffsetsDispatchDimensions::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"grid_min_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"grid_max_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"grid_min_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"grid_max_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"grid_size_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(6): \"grid_size_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 6,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(7): \"cell_offsets_dispatch_dimensions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 7,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<
                            [_root::common::DispatchIndirectArgs; 3],
                        >() as _),
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CalculateCellOffsetsDispatchDimensions::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 1]) -> [wgpu::BindGroupLayout; 1] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("CalculateCellOffsetsDispatchDimensions::PipelineLayout"),
            bind_group_layouts: &[Some(&WgpuBindGroup0::get_bind_group_layout(device))],
            immediate_size: 0u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("calculate_cell_offsets_dispatch_dimensions.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct DispatchIndirectArgsX_naga_oil_mod_XMNXW23LPNYX {
    x: u32,
    y: u32,
    z: u32,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;
const MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX: u32 = 65535u;
const N_CELL_INDIRECT_DISPATCHES: u32 = 3u;

@group(0) @binding(0) 
var<uniform> particle_radius: f32;
@group(0) @binding(1) 
var<uniform> grid_min_x: f32;
@group(0) @binding(2) 
var<uniform> grid_max_x: f32;
@group(0) @binding(3) 
var<uniform> grid_min_y: f32;
@group(0) @binding(4) 
var<uniform> grid_max_y: f32;
@group(0) @binding(5) 
var<storage, read_write> grid_size_x: u32;
@group(0) @binding(6) 
var<storage, read_write> grid_size_y: u32;
@group(0) @binding(7) 
var<storage, read_write> cell_offsets_dispatch_dimensions: array<DispatchIndirectArgsX_naga_oil_mod_XMNXW23LPNYX, 3>;

fn div_ceilX_naga_oil_mod_XMNXW23LPNYX(a: u32, b: u32) -> u32 {
    return (((a + b) - 1u) / b);
}

@compute @workgroup_size(1, 1, 1) 
fn calculate_cell_offsets_dispatch_dimensions() {
    var i: u32 = 0u;

    let _e2 = particle_radius;
    let cell_size = (_e2 * 2f);
    let _e6 = grid_max_x;
    let _e8 = grid_min_x;
    grid_size_x = u32((floor(((_e6 - _e8) / cell_size)) + 1f));
    let _e17 = grid_max_y;
    let _e19 = grid_min_y;
    grid_size_y = u32((floor(((_e17 - _e19) / cell_size)) + 1f));
    let _e28 = grid_size_x;
    let _e30 = grid_size_y;
    let cell_count = (_e28 * _e30);
    let _e33 = div_ceilX_naga_oil_mod_XMNXW23LPNYX(cell_count, WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX);
    let _e35 = div_ceilX_naga_oil_mod_XMNXW23LPNYX(_e33, MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX);
    if (_e35 > N_CELL_INDIRECT_DISPATCHES) {
        return;
    }
    loop {
        let _e39 = i;
        if (_e39 < _e35) {
        } else {
            break;
        }
        {
            let _e42 = i;
            let current_nwg = min((_e33 - (MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX * _e42)), MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX);
            let _e48 = i;
            cell_offsets_dispatch_dimensions[_e48] = DispatchIndirectArgsX_naga_oil_mod_XMNXW23LPNYX(current_nwg, 1u, 1u);
        }
        continuing {
            let _e54 = i;
            i = (_e54 + 1u);
        }
    }
    return;
}
"#;
}
pub mod calculate_cell_offsets {
    use super::{_root, _root::*};
    pub mod compute {
        use super::{_root, _root::*};
        pub const CALCULATE_CELL_OFFSETS_WORKGROUP_SIZE: [u32; 3] = [64, 1, 1];
        pub fn create_calculate_cell_offsets_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline calculate_cell_offsets"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("calculate_cell_offsets"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_CALCULATE_CELL_OFFSETS: &str = "calculate_cell_offsets";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub grid_min_x: wgpu::BufferBinding<'a>,
        pub grid_min_y: wgpu::BufferBinding<'a>,
        pub grid_size_x: wgpu::BufferBinding<'a>,
        pub grid_size_y: wgpu::BufferBinding<'a>,
        pub cell_object_count: wgpu::BufferBinding<'a>,
        pub current_cell_offset: wgpu::BufferBinding<'a>,
        pub cell_offsets: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub grid_min_x: wgpu::BindGroupEntry<'a>,
        pub grid_min_y: wgpu::BindGroupEntry<'a>,
        pub grid_size_x: wgpu::BindGroupEntry<'a>,
        pub grid_size_y: wgpu::BindGroupEntry<'a>,
        pub cell_object_count: wgpu::BindGroupEntry<'a>,
        pub current_cell_offset: wgpu::BindGroupEntry<'a>,
        pub cell_offsets: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                grid_min_x: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_x),
                },
                grid_min_y: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_y),
                },
                grid_size_x: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_x),
                },
                grid_size_y: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_y),
                },
                cell_object_count: wgpu::BindGroupEntry {
                    binding: 6,
                    resource: wgpu::BindingResource::Buffer(params.cell_object_count),
                },
                current_cell_offset: wgpu::BindGroupEntry {
                    binding: 7,
                    resource: wgpu::BindingResource::Buffer(params.current_cell_offset),
                },
                cell_offsets: wgpu::BindGroupEntry {
                    binding: 8,
                    resource: wgpu::BindingResource::Buffer(params.cell_offsets),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 7] {
            [
                self.grid_min_x,
                self.grid_min_y,
                self.grid_size_x,
                self.grid_size_y,
                self.cell_object_count,
                self.current_cell_offset,
                self.cell_offsets,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CalculateCellOffsets::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(1): \"grid_min_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"grid_min_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"grid_size_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"grid_size_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(6): \"cell_object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 6,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(7): \"current_cell_offset\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 7,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(8): \"cell_offsets\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 8,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CalculateCellOffsets::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 1]) -> [wgpu::BindGroupLayout; 1] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("CalculateCellOffsets::PipelineLayout"),
            bind_group_layouts: &[Some(&WgpuBindGroup0::get_bind_group_layout(device))],
            immediate_size: 4u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("calculate_cell_offsets.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;

var<immediate> thread_offset: u32;
@group(0) @binding(1) 
var<uniform> grid_min_x: f32;
@group(0) @binding(2) 
var<uniform> grid_min_y: f32;
@group(0) @binding(4) 
var<uniform> grid_size_x: u32;
@group(0) @binding(5) 
var<uniform> grid_size_y: u32;
@group(0) @binding(6) 
var<storage> cell_object_count: array<u32>;
@group(0) @binding(7) 
var<storage, read_write> current_cell_offset: atomic<u32>;
@group(0) @binding(8) 
var<storage, read_write> cell_offsets: array<u32>;

@compute @workgroup_size(64, 1, 1) 
fn calculate_cell_offsets(@builtin(global_invocation_id) gid: vec3<u32>) {
    let _e3 = thread_offset;
    let cell_index = (gid.x + _e3);
    let _e6 = grid_size_x;
    let _e8 = grid_size_y;
    let cell_count = (_e6 * _e8);
    if (cell_index >= cell_count) {
        return;
    }
    let count = cell_object_count[cell_index];
    let _e17 = atomicAdd((&current_cell_offset), count);
    cell_offsets[cell_index] = _e17;
    return;
}
"#;
}
pub mod populate_grid_cells {
    use super::{_root, _root::*};
    pub mod compute {
        use super::{_root, _root::*};
        pub const POPULATE_OBJECT_CELLS_WORKGROUP_SIZE: [u32; 3] = [64, 1, 1];
        pub fn create_populate_object_cells_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline populate_object_cells"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("populate_object_cells"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_POPULATE_OBJECT_CELLS: &str = "populate_object_cells";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub object_count: wgpu::BufferBinding<'a>,
        pub grid_size_x: wgpu::BufferBinding<'a>,
        pub object_cells: wgpu::BufferBinding<'a>,
        pub cell_offsets: wgpu::BufferBinding<'a>,
        pub cells: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub object_count: wgpu::BindGroupEntry<'a>,
        pub grid_size_x: wgpu::BindGroupEntry<'a>,
        pub object_cells: wgpu::BindGroupEntry<'a>,
        pub cell_offsets: wgpu::BindGroupEntry<'a>,
        pub cells: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                object_count: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.object_count),
                },
                grid_size_x: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_x),
                },
                object_cells: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.object_cells),
                },
                cell_offsets: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.cell_offsets),
                },
                cells: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.cells),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 5] {
            [
                self.object_count,
                self.grid_size_x,
                self.object_cells,
                self.cell_offsets,
                self.cells,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("PopulateGridCells::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"grid_size_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"object_cells\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"cell_offsets\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"cells\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("PopulateGridCells::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 1]) -> [wgpu::BindGroupLayout; 1] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("PopulateGridCells::PipelineLayout"),
            bind_group_layouts: &[Some(&WgpuBindGroup0::get_bind_group_layout(device))],
            immediate_size: 4u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("populate_grid_cells.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct CellPositionX_naga_oil_mod_XMNXW23LPNYX {
    cell: vec2<u32>,
    offset: u32,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;

var<immediate> thread_offset: u32;
@group(0) @binding(0) 
var<uniform> object_count: u32;
@group(0) @binding(1) 
var<uniform> grid_size_x: u32;
@group(0) @binding(2) 
var<storage> object_cells: array<CellPositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(0) @binding(3) 
var<storage> cell_offsets: array<u32>;
@group(0) @binding(4) 
var<storage, read_write> cells: array<u32>;

@compute @workgroup_size(64, 1, 1) 
fn populate_object_cells(@builtin(global_invocation_id) gid: vec3<u32>) {
    let _e3 = thread_offset;
    let object_index = (gid.x + _e3);
    let _e6 = object_count;
    if (object_index >= _e6) {
        return;
    }
    let cell_position = object_cells[object_index];
    let _e16 = grid_size_x;
    let cell_index = (cell_position.cell.x + (cell_position.cell.y * _e16));
    let cell_offset = cell_offsets[cell_index];
    cells[(cell_offset + cell_position.offset)] = object_index;
    return;
}
"#;
}
pub mod collision_broad_phase_grid {
    use super::{_root, _root::*};
    pub mod compute {
        use super::{_root, _root::*};
        pub const BROAD_PHASE_GRID_WORKGROUP_SIZE: [u32; 3] = [64, 1, 1];
        pub fn create_broad_phase_grid_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline broad_phase_grid"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("broad_phase_grid"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_BROAD_PHASE_GRID: &str = "broad_phase_grid";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub object_count: wgpu::BufferBinding<'a>,
        pub particle_radius: wgpu::BufferBinding<'a>,
        pub grid_min_x: wgpu::BufferBinding<'a>,
        pub grid_min_y: wgpu::BufferBinding<'a>,
        pub grid_size_x: wgpu::BufferBinding<'a>,
        pub grid_size_y: wgpu::BufferBinding<'a>,
        pub object_cells: wgpu::BufferBinding<'a>,
        pub cell_object_count: wgpu::BufferBinding<'a>,
        pub cell_offsets: wgpu::BufferBinding<'a>,
        pub cells: wgpu::BufferBinding<'a>,
        pub candidates: wgpu::BufferBinding<'a>,
        pub candidate_count: wgpu::BufferBinding<'a>,
        pub masses: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub object_count: wgpu::BindGroupEntry<'a>,
        pub particle_radius: wgpu::BindGroupEntry<'a>,
        pub grid_min_x: wgpu::BindGroupEntry<'a>,
        pub grid_min_y: wgpu::BindGroupEntry<'a>,
        pub grid_size_x: wgpu::BindGroupEntry<'a>,
        pub grid_size_y: wgpu::BindGroupEntry<'a>,
        pub object_cells: wgpu::BindGroupEntry<'a>,
        pub cell_object_count: wgpu::BindGroupEntry<'a>,
        pub cell_offsets: wgpu::BindGroupEntry<'a>,
        pub cells: wgpu::BindGroupEntry<'a>,
        pub candidates: wgpu::BindGroupEntry<'a>,
        pub candidate_count: wgpu::BindGroupEntry<'a>,
        pub masses: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                object_count: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.object_count),
                },
                particle_radius: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
                grid_min_x: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_x),
                },
                grid_min_y: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.grid_min_y),
                },
                grid_size_x: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_x),
                },
                grid_size_y: wgpu::BindGroupEntry {
                    binding: 6,
                    resource: wgpu::BindingResource::Buffer(params.grid_size_y),
                },
                object_cells: wgpu::BindGroupEntry {
                    binding: 7,
                    resource: wgpu::BindingResource::Buffer(params.object_cells),
                },
                cell_object_count: wgpu::BindGroupEntry {
                    binding: 8,
                    resource: wgpu::BindingResource::Buffer(params.cell_object_count),
                },
                cell_offsets: wgpu::BindGroupEntry {
                    binding: 9,
                    resource: wgpu::BindingResource::Buffer(params.cell_offsets),
                },
                cells: wgpu::BindGroupEntry {
                    binding: 10,
                    resource: wgpu::BindingResource::Buffer(params.cells),
                },
                candidates: wgpu::BindGroupEntry {
                    binding: 11,
                    resource: wgpu::BindingResource::Buffer(params.candidates),
                },
                candidate_count: wgpu::BindGroupEntry {
                    binding: 12,
                    resource: wgpu::BindingResource::Buffer(params.candidate_count),
                },
                masses: wgpu::BindGroupEntry {
                    binding: 13,
                    resource: wgpu::BindingResource::Buffer(params.masses),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 13] {
            [
                self.object_count,
                self.particle_radius,
                self.grid_min_x,
                self.grid_min_y,
                self.grid_size_x,
                self.grid_size_y,
                self.object_cells,
                self.cell_object_count,
                self.cell_offsets,
                self.cells,
                self.candidates,
                self.candidate_count,
                self.masses,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionBroadPhaseGrid::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"grid_min_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"grid_min_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"grid_size_x\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(6): \"grid_size_y\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 6,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(7): \"object_cells\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 7,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(8): \"cell_object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 8,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(9): \"cell_offsets\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 9,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(10): \"cells\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 10,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(11): \"candidates\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 11,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(12): \"candidate_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 12,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(13): \"masses\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 13,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionBroadPhaseGrid::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1EntriesParams<'a> {
        pub positions: wgpu::BufferBinding<'a>,
        pub flags: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup1Entries<'a> {
        pub positions: wgpu::BindGroupEntry<'a>,
        pub flags: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup1Entries<'a> {
        pub fn new(params: WgpuBindGroup1EntriesParams<'a>) -> Self {
            Self {
                positions: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.positions),
                },
                flags: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.flags),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 2] {
            [self.positions, self.flags]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1(wgpu::BindGroup);
    impl WgpuBindGroup1 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionBroadPhaseGrid::BindGroup1::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(1): \"positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"flags\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup1Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionBroadPhaseGrid::BindGroup1"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(1, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup2EntriesParams<'a> {
        pub forces: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup2Entries<'a> {
        pub forces: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup2Entries<'a> {
        pub fn new(params: WgpuBindGroup2EntriesParams<'a>) -> Self {
            Self {
                forces: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.forces),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 1] {
            [self.forces]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup2(wgpu::BindGroup);
    impl WgpuBindGroup2 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionBroadPhaseGrid::BindGroup2::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"forces\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup2Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionBroadPhaseGrid::BindGroup2"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(2, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
        pub bind_group1: &'a WgpuBindGroup1,
        pub bind_group2: &'a WgpuBindGroup2,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
            self.bind_group1.set(pass);
            self.bind_group2.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 3]) -> [wgpu::BindGroupLayout; 3] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("CollisionBroadPhaseGrid::PipelineLayout"),
            bind_group_layouts: &[
                Some(&WgpuBindGroup0::get_bind_group_layout(device)),
                Some(&WgpuBindGroup1::get_bind_group_layout(device)),
                Some(&WgpuBindGroup2::get_bind_group_layout(device)),
            ],
            immediate_size: 4u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("collision_broad_phase_grid.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct FlagsX_naga_oil_mod_XMNXW23LPNYX {
    inner: u32,
}

struct MassX_naga_oil_mod_XMNXW23LPNYX {
    inner: f32,
}

struct PositionX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct CollisionCandidateX_naga_oil_mod_XMNXW23LPNYX {
    a: u32,
    b: u32,
}

struct CellPositionX_naga_oil_mod_XMNXW23LPNYX {
    cell: vec2<u32>,
    offset: u32,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;
const FLAG_PHYSICALX_naga_oil_mod_XMNXW23LPNYX: u32 = 4u;
const FLAG_COLLISIONX_naga_oil_mod_XMNXW23LPNYX: u32 = 16u;
const MAX_CANDIDATES_PER_OBJECTX_naga_oil_mod_XMNXW23LPNYX: u32 = 16u;

var<immediate> thread_offset: u32;
@group(0) @binding(0) 
var<uniform> object_count: u32;
@group(0) @binding(1) 
var<uniform> particle_radius: f32;
@group(0) @binding(2) 
var<uniform> grid_min_x: f32;
@group(0) @binding(3) 
var<uniform> grid_min_y: f32;
@group(0) @binding(5) 
var<uniform> grid_size_x: u32;
@group(0) @binding(6) 
var<uniform> grid_size_y: u32;
@group(0) @binding(7) 
var<storage> object_cells: array<CellPositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(0) @binding(8) 
var<storage> cell_object_count: array<u32>;
@group(0) @binding(9) 
var<storage> cell_offsets: array<u32>;
@group(0) @binding(10) 
var<storage> cells: array<u32>;
@group(0) @binding(11) 
var<storage, read_write> candidates: array<CollisionCandidateX_naga_oil_mod_XMNXW23LPNYX>;
@group(0) @binding(12) 
var<storage, read_write> candidate_count: atomic<u32>;
@group(0) @binding(13) 
var<storage> masses: array<MassX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(1) 
var<storage> positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(2) 
var<storage> flags: array<FlagsX_naga_oil_mod_XMNXW23LPNYX>;
@group(2) @binding(0) 
var<storage, read_write> forces: array<atomic<u32>>;

fn cas_add_force_component(i_1: u32, value: f32) {
    var old: u32;

    let _e3 = atomicLoad((&forces[i_1]));
    old = _e3;
    loop {
        let _e5 = old;
        let old_f = bitcast<f32>(_e5);
        let new_f = (old_f + value);
        let new_value = bitcast<u32>(new_f);
        let _e12 = old;
        let _e13 = atomicCompareExchangeWeak((&forces[i_1]), _e12, new_value);
        if _e13.exchanged {
            break;
        }
        old = _e13.old_value;
    }
    return;
}

fn cas_add_force(i_2: u32, value_1: vec2<f32>) {
    cas_add_force_component((i_2 * 2u), value_1.x);
    cas_add_force_component(((i_2 * 2u) + 1u), value_1.y);
    return;
}

@compute @workgroup_size(64, 1, 1) 
fn broad_phase_grid(@builtin(global_invocation_id) gid: vec3<u32>, @builtin(local_invocation_index) local_invocation_index: u32) {
    var local: bool;
    var i: u32;
    var j: u32;
    var k: u32;
    var local_1: bool;

    let _e3 = thread_offset;
    let object_index = (gid.x + _e3);
    let _e6 = object_count;
    if !((object_index >= _e6)) {
        let _e12 = flags[object_index].inner;
        local = ((_e12 & FLAG_PHYSICALX_naga_oil_mod_XMNXW23LPNYX) == 0u);
    } else {
        local = true;
    }
    let _e20 = local;
    if _e20 {
        return;
    }
    let c1_ = positions[object_index].inner;
    let _e26 = object_count;
    let max_candidates = (_e26 * MAX_CANDIDATES_PER_OBJECTX_naga_oil_mod_XMNXW23LPNYX);
    let _e32 = object_cells[object_index].cell;
    let cell = vec2<i32>(_e32);
    let min_cell = vec2<u32>(max(vec2<i32>(), (cell - vec2(1i))));
    let _e43 = grid_size_x;
    let _e47 = grid_size_y;
    let max_cell = vec2<u32>(min((cell + vec2(1i)), vec2<i32>(vec2<u32>((_e43 - 1u), (_e47 - 1u)))));
    i = min_cell.x;
    loop {
        let _e56 = i;
        if (_e56 <= max_cell.x) {
        } else {
            break;
        }
        {
            j = min_cell.y;
            loop {
                let _e61 = j;
                if (_e61 <= max_cell.y) {
                } else {
                    break;
                }
                {
                    let _e64 = i;
                    let _e65 = j;
                    let _e67 = grid_size_x;
                    let cell_index = (_e64 + (_e65 * _e67));
                    let object_count_1 = cell_object_count[cell_index];
                    if (object_count_1 == 0u) {
                        continue;
                    }
                    let cell_offset = cell_offsets[cell_index];
                    k = 0u;
                    loop {
                        let _e80 = k;
                        if (_e80 < object_count_1) {
                        } else {
                            break;
                        }
                        {
                            let _e83 = k;
                            let other_object_index = cells[(cell_offset + _e83)];
                            if (other_object_index >= object_index) {
                                continue;
                            }
                            let c2_ = positions[other_object_index].inner;
                            let _e95 = flags[other_object_index].inner;
                            if ((_e95 & FLAG_COLLISIONX_naga_oil_mod_XMNXW23LPNYX) == 0u) {
                                continue;
                            }
                            let delta = (c1_ - c2_);
                            let distance_squared = dot(delta, delta);
                            let _e103 = particle_radius;
                            let particle_size = (_e103 * 2f);
                            let particle_size_squared = (particle_size * particle_size);
                            if !((distance_squared > particle_size_squared)) {
                                local_1 = (distance_squared < 0.0000000001f);
                            } else {
                                local_1 = true;
                            }
                            let _e114 = local_1;
                            if _e114 {
                                continue;
                            }
                            let _e117 = atomicAdd((&candidate_count), 1u);
                            if (_e117 >= max_candidates) {
                                return;
                            }
                            candidates[_e117] = CollisionCandidateX_naga_oil_mod_XMNXW23LPNYX(object_index, other_object_index);
                        }
                        continuing {
                            let _e123 = k;
                            k = (_e123 + 1u);
                        }
                    }
                }
                continuing {
                    let _e126 = j;
                    j = (_e126 + 1u);
                }
            }
        }
        continuing {
            let _e129 = i;
            i = (_e129 + 1u);
        }
    }
    return;
}
"#;
}
pub mod collision_narrow_phase_dispatch_dimensions {
    use super::{_root, _root::*};
    pub mod compute {
        use super::{_root, _root::*};
        pub const CALCULATE_NARROW_PHASE_DISPATCH_DIMENSIONS_WORKGROUP_SIZE: [u32; 3] = [1, 1, 1];
        pub fn create_calculate_narrow_phase_dispatch_dimensions_pipeline_embed_source(
            device: &wgpu::Device,
        ) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline calculate_narrow_phase_dispatch_dimensions"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("calculate_narrow_phase_dispatch_dimensions"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_CALCULATE_NARROW_PHASE_DISPATCH_DIMENSIONS: &str = "calculate_narrow_phase_dispatch_dimensions";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub candidate_count: wgpu::BufferBinding<'a>,
        pub narrow_phase_dispatch_dimensions: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub candidate_count: wgpu::BindGroupEntry<'a>,
        pub narrow_phase_dispatch_dimensions: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                candidate_count: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.candidate_count),
                },
                narrow_phase_dispatch_dimensions: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.narrow_phase_dispatch_dimensions),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 2] {
            [self.candidate_count, self.narrow_phase_dispatch_dimensions]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionNarrowPhaseDispatchDimensions::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"candidate_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"narrow_phase_dispatch_dimensions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<
                            _root::common::DispatchIndirectArgs,
                        >() as _),
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionNarrowPhaseDispatchDimensions::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 1]) -> [wgpu::BindGroupLayout; 1] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("CollisionNarrowPhaseDispatchDimensions::PipelineLayout"),
            bind_group_layouts: &[Some(&WgpuBindGroup0::get_bind_group_layout(device))],
            immediate_size: 0u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("collision_narrow_phase_dispatch_dimensions.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct DispatchIndirectArgsX_naga_oil_mod_XMNXW23LPNYX {
    x: u32,
    y: u32,
    z: u32,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;
const MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX: u32 = 65535u;

@group(0) @binding(0) 
var<uniform> candidate_count: u32;
@group(0) @binding(1) 
var<storage, read_write> narrow_phase_dispatch_dimensions: DispatchIndirectArgsX_naga_oil_mod_XMNXW23LPNYX;

fn div_ceilX_naga_oil_mod_XMNXW23LPNYX(a: u32, b: u32) -> u32 {
    return (((a + b) - 1u) / b);
}

@compute @workgroup_size(1, 1, 1) 
fn calculate_narrow_phase_dispatch_dimensions() {
    let _e1 = candidate_count;
    let _e3 = div_ceilX_naga_oil_mod_XMNXW23LPNYX(_e1, WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX);
    let x = min(_e3, MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX);
    let y = min((((_e3 + x) - 1u) / x), MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX);
    let z = min((((_e3 + (x * y)) - 1u) / (x * y)), MAX_DISPATCH_DIMENSIONX_naga_oil_mod_XMNXW23LPNYX);
    narrow_phase_dispatch_dimensions = DispatchIndirectArgsX_naga_oil_mod_XMNXW23LPNYX(x, y, z);
    return;
}
"#;
}
pub mod collision_narrow_phase {
    use super::{_root, _root::*};
    pub const STIFFNESS: f32 = 100000f32;
    pub const RESTITUTION: f32 = 0f32;
    pub const GAMMA_COEFF: f32 = 212.13202f32;
    pub mod compute {
        use super::{_root, _root::*};
        pub const NARROW_PHASE_WORKGROUP_SIZE: [u32; 3] = [64, 1, 1];
        pub fn create_narrow_phase_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline narrow_phase"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("narrow_phase"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_NARROW_PHASE: &str = "narrow_phase";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub stiffness: wgpu::BufferBinding<'a>,
        pub restitution: wgpu::BufferBinding<'a>,
        pub particle_radius: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub stiffness: wgpu::BindGroupEntry<'a>,
        pub restitution: wgpu::BindGroupEntry<'a>,
        pub particle_radius: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                stiffness: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.stiffness),
                },
                restitution: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.restitution),
                },
                particle_radius: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 3] {
            [self.stiffness, self.restitution, self.particle_radius]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionNarrowPhase::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"stiffness\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"restitution\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionNarrowPhase::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1EntriesParams<'a> {
        pub positions: wgpu::BufferBinding<'a>,
        pub velocities: wgpu::BufferBinding<'a>,
        pub masses: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup1Entries<'a> {
        pub positions: wgpu::BindGroupEntry<'a>,
        pub velocities: wgpu::BindGroupEntry<'a>,
        pub masses: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup1Entries<'a> {
        pub fn new(params: WgpuBindGroup1EntriesParams<'a>) -> Self {
            Self {
                positions: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.positions),
                },
                velocities: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.velocities),
                },
                masses: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.masses),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 3] {
            [self.positions, self.velocities, self.masses]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1(wgpu::BindGroup);
    impl WgpuBindGroup1 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionNarrowPhase::BindGroup1::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"velocities\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"masses\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup1Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionNarrowPhase::BindGroup1"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(1, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup2EntriesParams<'a> {
        pub candidate_count: wgpu::BufferBinding<'a>,
        pub candidates: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup2Entries<'a> {
        pub candidate_count: wgpu::BindGroupEntry<'a>,
        pub candidates: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup2Entries<'a> {
        pub fn new(params: WgpuBindGroup2EntriesParams<'a>) -> Self {
            Self {
                candidate_count: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.candidate_count),
                },
                candidates: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.candidates),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 2] {
            [self.candidate_count, self.candidates]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup2(wgpu::BindGroup);
    impl WgpuBindGroup2 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionNarrowPhase::BindGroup2::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"candidate_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"candidates\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup2Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionNarrowPhase::BindGroup2"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(2, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup3EntriesParams<'a> {
        pub forces: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup3Entries<'a> {
        pub forces: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup3Entries<'a> {
        pub fn new(params: WgpuBindGroup3EntriesParams<'a>) -> Self {
            Self {
                forces: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.forces),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 1] {
            [self.forces]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup3(wgpu::BindGroup);
    impl WgpuBindGroup3 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("CollisionNarrowPhase::BindGroup3::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"forces\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup3Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("CollisionNarrowPhase::BindGroup3"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(3, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
        pub bind_group1: &'a WgpuBindGroup1,
        pub bind_group2: &'a WgpuBindGroup2,
        pub bind_group3: &'a WgpuBindGroup3,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
            self.bind_group1.set(pass);
            self.bind_group2.set(pass);
            self.bind_group3.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 4]) -> [wgpu::BindGroupLayout; 4] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("CollisionNarrowPhase::PipelineLayout"),
            bind_group_layouts: &[
                Some(&WgpuBindGroup0::get_bind_group_layout(device)),
                Some(&WgpuBindGroup1::get_bind_group_layout(device)),
                Some(&WgpuBindGroup2::get_bind_group_layout(device)),
                Some(&WgpuBindGroup3::get_bind_group_layout(device)),
            ],
            immediate_size: 0u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("collision_narrow_phase.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct VelocityX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct MassX_naga_oil_mod_XMNXW23LPNYX {
    inner: f32,
}

struct PositionX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct CollisionCandidateX_naga_oil_mod_XMNXW23LPNYX {
    a: u32,
    b: u32,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;
const STIFFNESS: f32 = 100000f;
const RESTITUTION: f32 = 0f;
const GAMMA_COEFF: f32 = 212.13202f;

@group(0) @binding(0) 
var<uniform> stiffness: f32;
@group(0) @binding(1) 
var<uniform> restitution: f32;
@group(0) @binding(2) 
var<uniform> particle_radius: f32;
@group(1) @binding(0) 
var<storage> positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(1) 
var<storage> velocities: array<VelocityX_naga_oil_mod_XMNXW23LPNYX>;
@group(1) @binding(2) 
var<storage> masses: array<MassX_naga_oil_mod_XMNXW23LPNYX>;
@group(2) @binding(0) 
var<uniform> candidate_count: u32;
@group(2) @binding(1) 
var<storage> candidates: array<CollisionCandidateX_naga_oil_mod_XMNXW23LPNYX>;
@group(3) @binding(0) 
var<storage, read_write> forces: array<atomic<u32>>;

fn flat_invocation_indexX_naga_oil_mod_XMNXW23LPNYX(gid_1: vec3<u32>, nwg_1: vec3<u32>, workgroup_size: u32) -> u32 {
    return ((gid_1.x + ((gid_1.y * workgroup_size) * nwg_1.x)) + ((((gid_1.z * workgroup_size) * nwg_1.x) * workgroup_size) * nwg_1.y));
}

fn effective_mass(m1_: f32, m2_: f32) -> f32 {
    if (m1_ == 0f) {
        return m2_;
    }
    if (m2_ == 0f) {
        return m1_;
    }
    return ((m1_ * m2_) / (m1_ + m2_));
}

fn collision_repulsion_pair(x1_: PositionX_naga_oil_mod_XMNXW23LPNYX, v1_: vec2<f32>, m1_1: f32, x2_: PositionX_naga_oil_mod_XMNXW23LPNYX, v2_: vec2<f32>, m2_1: f32) -> vec2<f32> {
    var f_elastic: vec2<f32> = vec2<f32>();
    var f_damping: vec2<f32> = vec2<f32>();

    let separation_vector = (x1_.inner - x2_.inner);
    let distance_ = length(separation_vector);
    let n = (separation_vector / vec2(distance_));
    let _e10 = particle_radius;
    let interaction_distance = (_e10 * 2f);
    let penetration = (interaction_distance - distance_);
    f_elastic = ((STIFFNESS * max(0f, penetration)) * n);
    let v_rel = (v1_ - v2_);
    let v_n = dot(v_rel, n);
    let _e26 = effective_mass(m1_1, m2_1);
    f_damping = (((-212.13202f * sqrt(_e26)) * min(0f, v_n)) * n);
    let _e35 = f_elastic;
    let _e36 = f_damping;
    return (_e35 + _e36);
}

fn cas_add_force_component(i: u32, value: f32) {
    var old: u32;

    let _e3 = atomicLoad((&forces[i]));
    old = _e3;
    loop {
        let _e5 = old;
        let old_f = bitcast<f32>(_e5);
        let new_f = (old_f + value);
        let new_value = bitcast<u32>(new_f);
        let _e12 = old;
        let _e13 = atomicCompareExchangeWeak((&forces[i]), _e12, new_value);
        if _e13.exchanged {
            break;
        }
        old = _e13.old_value;
    }
    return;
}

fn cas_add_force(i_1: u32, value_1: vec2<f32>) {
    cas_add_force_component((i_1 * 2u), value_1.x);
    cas_add_force_component(((i_1 * 2u) + 1u), value_1.y);
    return;
}

@compute @workgroup_size(64, 1, 1) 
fn narrow_phase(@builtin(global_invocation_id) gid: vec3<u32>, @builtin(num_workgroups) nwg: vec3<u32>, @builtin(local_invocation_index) local_invocation_index: u32) {
    let _e3 = flat_invocation_indexX_naga_oil_mod_XMNXW23LPNYX(gid, nwg, WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX);
    let _e5 = candidate_count;
    if (_e3 >= _e5) {
        return;
    }
    let candidates_1 = candidates[_e3];
    let a = candidates_1.a;
    let b = candidates_1.b;
    let _e14 = positions[a];
    let _e18 = velocities[a].inner;
    let _e22 = masses[a].inner;
    let _e25 = positions[b];
    let _e29 = velocities[b].inner;
    let _e33 = masses[b].inner;
    let _e34 = collision_repulsion_pair(_e14, _e18, _e22, _e25, _e29, _e33);
    cas_add_force(a, _e34);
    cas_add_force(b, -(_e34));
    return;
}
"#;
}
pub mod integrate {
    use super::{_root, _root::*};
    #[repr(C, align(8))]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct BlackHole {
        #[doc = "offset: 0, size: 8, type: `vec2<f32>`"]
        pub position: [f32; 2],
        #[doc = "offset: 8, size: 4, type: `f32`"]
        pub radius: f32,
        #[doc = "offset: 12, size: 4, type: `f32`"]
        pub mass: f32,
        #[doc = "offset: 16, size: 4, type: `f32`"]
        pub spin: f32,
        pub _pad_spin: [u8; 0x4],
    }
    impl BlackHole {
        pub const fn new(position: [f32; 2], radius: f32, mass: f32, spin: f32) -> Self {
            Self {
                position,
                radius,
                mass,
                spin,
                _pad_spin: [0; 0x4],
            }
        }
    }
    #[repr(C)]
    #[derive(Debug, PartialEq, Clone, Copy)]
    pub struct BlackHoleInit {
        pub position: [f32; 2],
        pub radius: f32,
        pub mass: f32,
        pub spin: f32,
    }
    impl BlackHoleInit {
        pub fn build(&self) -> BlackHole {
            BlackHole {
                position: self.position,
                radius: self.radius,
                mass: self.mass,
                spin: self.spin,
                _pad_spin: [0; 0x4],
            }
        }
    }
    impl From<BlackHoleInit> for BlackHole {
        fn from(data: BlackHoleInit) -> Self {
            data.build()
        }
    }
    pub mod compute {
        use super::{_root, _root::*};
        pub const INTEGRATE_WORKGROUP_SIZE: [u32; 3] = [64, 1, 1];
        pub fn create_integrate_pipeline_embed_source(device: &wgpu::Device) -> wgpu::ComputePipeline {
            let module = super::create_shader_module_embed_source(device);
            let layout = super::create_pipeline_layout(device);
            device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
                label: Some("Compute Pipeline integrate"),
                layout: Some(&layout),
                module: &module,
                entry_point: Some("integrate"),
                compilation_options: Default::default(),
                cache: None,
            })
        }
    }
    pub const ENTRY_INTEGRATE: &str = "integrate";
    #[derive(Debug)]
    pub struct WgpuBindGroup0EntriesParams<'a> {
        pub dt: wgpu::BufferBinding<'a>,
        pub gravitational_constant: wgpu::BufferBinding<'a>,
        pub global_acceleration: wgpu::BufferBinding<'a>,
        pub object_count: wgpu::BufferBinding<'a>,
        pub particle_radius: wgpu::BufferBinding<'a>,
        pub constraints: wgpu::BufferBinding<'a>,
        pub masses: wgpu::BufferBinding<'a>,
        pub forces: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup0Entries<'a> {
        pub dt: wgpu::BindGroupEntry<'a>,
        pub gravitational_constant: wgpu::BindGroupEntry<'a>,
        pub global_acceleration: wgpu::BindGroupEntry<'a>,
        pub object_count: wgpu::BindGroupEntry<'a>,
        pub particle_radius: wgpu::BindGroupEntry<'a>,
        pub constraints: wgpu::BindGroupEntry<'a>,
        pub masses: wgpu::BindGroupEntry<'a>,
        pub forces: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup0Entries<'a> {
        pub fn new(params: WgpuBindGroup0EntriesParams<'a>) -> Self {
            Self {
                dt: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.dt),
                },
                gravitational_constant: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.gravitational_constant),
                },
                global_acceleration: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.global_acceleration),
                },
                object_count: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.object_count),
                },
                particle_radius: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.particle_radius),
                },
                constraints: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.constraints),
                },
                masses: wgpu::BindGroupEntry {
                    binding: 6,
                    resource: wgpu::BindingResource::Buffer(params.masses),
                },
                forces: wgpu::BindGroupEntry {
                    binding: 7,
                    resource: wgpu::BindingResource::Buffer(params.forces),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 8] {
            [
                self.dt,
                self.gravitational_constant,
                self.global_acceleration,
                self.object_count,
                self.particle_radius,
                self.constraints,
                self.masses,
                self.forces,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup0(wgpu::BindGroup);
    impl WgpuBindGroup0 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("Integrate::BindGroup0::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"dt\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"gravitational_constant\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"global_acceleration\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<[f32; 2]>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"object_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"particle_radius\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"constraints\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<_root::common::AABB>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(6): \"masses\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 6,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(7): \"forces\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 7,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup0Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("Integrate::BindGroup0"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(0, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1EntriesParams<'a> {
        pub blackhole_count: wgpu::BufferBinding<'a>,
        pub blackhole_mass_scale: wgpu::BufferBinding<'a>,
        pub blackhole_size_scale: wgpu::BufferBinding<'a>,
        pub blackholes: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup1Entries<'a> {
        pub blackhole_count: wgpu::BindGroupEntry<'a>,
        pub blackhole_mass_scale: wgpu::BindGroupEntry<'a>,
        pub blackhole_size_scale: wgpu::BindGroupEntry<'a>,
        pub blackholes: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup1Entries<'a> {
        pub fn new(params: WgpuBindGroup1EntriesParams<'a>) -> Self {
            Self {
                blackhole_count: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.blackhole_count),
                },
                blackhole_mass_scale: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.blackhole_mass_scale),
                },
                blackhole_size_scale: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.blackhole_size_scale),
                },
                blackholes: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.blackholes),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 4] {
            [
                self.blackhole_count,
                self.blackhole_mass_scale,
                self.blackhole_size_scale,
                self.blackholes,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup1(wgpu::BindGroup);
    impl WgpuBindGroup1 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("Integrate::BindGroup1::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"blackhole_count\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<u32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"blackhole_mass_scale\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"blackhole_size_scale\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Uniform,
                        has_dynamic_offset: false,
                        min_binding_size: std::num::NonZeroU64::new(std::mem::size_of::<f32>() as _),
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"blackholes\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup1Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("Integrate::BindGroup1"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(1, &self.0, &[]);
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup2EntriesParams<'a> {
        pub flags: wgpu::BufferBinding<'a>,
        pub positions: wgpu::BufferBinding<'a>,
        pub velocities: wgpu::BufferBinding<'a>,
        pub integrated_flags: wgpu::BufferBinding<'a>,
        pub integrated_positions: wgpu::BufferBinding<'a>,
        pub integrated_velocities: wgpu::BufferBinding<'a>,
    }
    #[derive(Clone, Debug)]
    pub struct WgpuBindGroup2Entries<'a> {
        pub flags: wgpu::BindGroupEntry<'a>,
        pub positions: wgpu::BindGroupEntry<'a>,
        pub velocities: wgpu::BindGroupEntry<'a>,
        pub integrated_flags: wgpu::BindGroupEntry<'a>,
        pub integrated_positions: wgpu::BindGroupEntry<'a>,
        pub integrated_velocities: wgpu::BindGroupEntry<'a>,
    }
    impl<'a> WgpuBindGroup2Entries<'a> {
        pub fn new(params: WgpuBindGroup2EntriesParams<'a>) -> Self {
            Self {
                flags: wgpu::BindGroupEntry {
                    binding: 0,
                    resource: wgpu::BindingResource::Buffer(params.flags),
                },
                positions: wgpu::BindGroupEntry {
                    binding: 1,
                    resource: wgpu::BindingResource::Buffer(params.positions),
                },
                velocities: wgpu::BindGroupEntry {
                    binding: 2,
                    resource: wgpu::BindingResource::Buffer(params.velocities),
                },
                integrated_flags: wgpu::BindGroupEntry {
                    binding: 3,
                    resource: wgpu::BindingResource::Buffer(params.integrated_flags),
                },
                integrated_positions: wgpu::BindGroupEntry {
                    binding: 4,
                    resource: wgpu::BindingResource::Buffer(params.integrated_positions),
                },
                integrated_velocities: wgpu::BindGroupEntry {
                    binding: 5,
                    resource: wgpu::BindingResource::Buffer(params.integrated_velocities),
                },
            }
        }
        pub fn into_array(self) -> [wgpu::BindGroupEntry<'a>; 6] {
            [
                self.flags,
                self.positions,
                self.velocities,
                self.integrated_flags,
                self.integrated_positions,
                self.integrated_velocities,
            ]
        }
        pub fn collect<B: FromIterator<wgpu::BindGroupEntry<'a>>>(self) -> B {
            self.into_array().into_iter().collect()
        }
    }
    #[derive(Debug)]
    pub struct WgpuBindGroup2(wgpu::BindGroup);
    impl WgpuBindGroup2 {
        pub const LAYOUT_DESCRIPTOR: wgpu::BindGroupLayoutDescriptor<'static> = wgpu::BindGroupLayoutDescriptor {
            label: Some("Integrate::BindGroup2::LayoutDescriptor"),
            entries: &[
                #[doc = " @binding(0): \"flags\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 0,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(1): \"positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 1,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(2): \"velocities\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 2,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: true },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(3): \"integrated_flags\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 3,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(4): \"integrated_positions\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 4,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
                #[doc = " @binding(5): \"integrated_velocities\""]
                wgpu::BindGroupLayoutEntry {
                    binding: 5,
                    visibility: wgpu::ShaderStages::COMPUTE,
                    ty: wgpu::BindingType::Buffer {
                        ty: wgpu::BufferBindingType::Storage { read_only: false },
                        has_dynamic_offset: false,
                        min_binding_size: None,
                    },
                    count: None,
                },
            ],
        };
        pub fn get_bind_group_layout(device: &wgpu::Device) -> wgpu::BindGroupLayout {
            device.create_bind_group_layout(&Self::LAYOUT_DESCRIPTOR)
        }
        pub fn from_bindings(device: &wgpu::Device, bindings: WgpuBindGroup2Entries) -> Self {
            let bind_group_layout = Self::get_bind_group_layout(device);
            let entries = bindings.into_array();
            let bind_group = device.create_bind_group(&wgpu::BindGroupDescriptor {
                label: Some("Integrate::BindGroup2"),
                layout: &bind_group_layout,
                entries: &entries,
            });
            Self(bind_group)
        }
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            pass.set_bind_group(2, &self.0, &[]);
        }
    }
    #[doc = " Bind groups can be set individually using their set(render_pass) method, or all at once using `WgpuBindGroups::set`."]
    #[doc = " For optimal performance with many draw calls, it's recommended to organize bindings into bind groups based on update frequency:"]
    #[doc = "   - Bind group 0: Least frequent updates (e.g. per frame resources)"]
    #[doc = "   - Bind group 1: More frequent updates"]
    #[doc = "   - Bind group 2: More frequent updates"]
    #[doc = "   - Bind group 3: Most frequent updates (e.g. per draw resources)"]
    #[derive(Debug, Copy, Clone)]
    pub struct WgpuBindGroups<'a> {
        pub bind_group0: &'a WgpuBindGroup0,
        pub bind_group1: &'a WgpuBindGroup1,
        pub bind_group2: &'a WgpuBindGroup2,
    }
    impl<'a> WgpuBindGroups<'a> {
        pub fn set(&self, pass: &mut impl SetBindGroup) {
            self.bind_group0.set(pass);
            self.bind_group1.set(pass);
            self.bind_group2.set(pass);
        }
    }
    #[derive(Debug)]
    pub struct WgpuPipelineLayout;
    impl WgpuPipelineLayout {
        pub fn bind_group_layout_entries(entries: [wgpu::BindGroupLayout; 3]) -> [wgpu::BindGroupLayout; 3] {
            entries
        }
    }
    pub fn create_pipeline_layout(device: &wgpu::Device) -> wgpu::PipelineLayout {
        device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("Integrate::PipelineLayout"),
            bind_group_layouts: &[
                Some(&WgpuBindGroup0::get_bind_group_layout(device)),
                Some(&WgpuBindGroup1::get_bind_group_layout(device)),
                Some(&WgpuBindGroup2::get_bind_group_layout(device)),
            ],
            immediate_size: 4u32,
        })
    }
    pub fn create_shader_module_embed_source(device: &wgpu::Device) -> wgpu::ShaderModule {
        let source = std::borrow::Cow::Borrowed(SHADER_STRING);
        device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("integrate.wgsl"),
            source: wgpu::ShaderSource::Wgsl(source),
        })
    }
    pub const SHADER_STRING: &str = r#"
struct FlagsX_naga_oil_mod_XMNXW23LPNYX {
    inner: u32,
}

struct VelocityX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct MassX_naga_oil_mod_XMNXW23LPNYX {
    inner: f32,
}

struct PositionX_naga_oil_mod_XMNXW23LPNYX {
    inner: vec2<f32>,
}

struct AABBX_naga_oil_mod_XMNXW23LPNYX {
    min: vec2<f32>,
    max: vec2<f32>,
}

struct BlackHole {
    position: vec2<f32>,
    radius: f32,
    mass: f32,
    spin: f32,
}

struct ObjectPhaseState {
    position: vec2<f32>,
    velocity: vec2<f32>,
}

const WORKGROUP_SIZEX_naga_oil_mod_XMNXW23LPNYX: u32 = 64u;
const FLAG_DRAW_OBJECTX_naga_oil_mod_XMNXW23LPNYX: u32 = 1u;
const FLAG_DRAW_AABBX_naga_oil_mod_XMNXW23LPNYX: u32 = 2u;
const FLAG_PHYSICALX_naga_oil_mod_XMNXW23LPNYX: u32 = 4u;
const FLAG_COLLISIONX_naga_oil_mod_XMNXW23LPNYX: u32 = 16u;

var<immediate> thread_offset: u32;
@group(0) @binding(0) 
var<uniform> dt: f32;
@group(0) @binding(1) 
var<uniform> gravitational_constant: f32;
@group(0) @binding(2) 
var<uniform> global_acceleration: vec2<f32>;
@group(0) @binding(3) 
var<uniform> object_count: u32;
@group(0) @binding(4) 
var<uniform> particle_radius: f32;
@group(0) @binding(5) 
var<uniform> constraints: AABBX_naga_oil_mod_XMNXW23LPNYX;
@group(0) @binding(6) 
var<storage> masses: array<MassX_naga_oil_mod_XMNXW23LPNYX>;
@group(0) @binding(7) 
var<storage> forces: array<u32>;
@group(1) @binding(0) 
var<uniform> blackhole_count: u32;
@group(1) @binding(1) 
var<uniform> blackhole_mass_scale: f32;
@group(1) @binding(2) 
var<uniform> blackhole_size_scale: f32;
@group(1) @binding(3) 
var<storage> blackholes: array<BlackHole>;
@group(2) @binding(0) 
var<storage> flags: array<FlagsX_naga_oil_mod_XMNXW23LPNYX>;
@group(2) @binding(1) 
var<storage> positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(2) @binding(2) 
var<storage> velocities: array<VelocityX_naga_oil_mod_XMNXW23LPNYX>;
@group(2) @binding(3) 
var<storage, read_write> integrated_flags: array<FlagsX_naga_oil_mod_XMNXW23LPNYX>;
@group(2) @binding(4) 
var<storage, read_write> integrated_positions: array<PositionX_naga_oil_mod_XMNXW23LPNYX>;
@group(2) @binding(5) 
var<storage, read_write> integrated_velocities: array<VelocityX_naga_oil_mod_XMNXW23LPNYX>;

fn blackhole_gravity(blackhole: BlackHole, position: vec2<f32>, mass: f32) -> vec2<f32> {
    let to_blackhole = (blackhole.position - position);
    let direction = normalize(to_blackhole);
    let distance_ = length(to_blackhole);
    let _e8 = gravitational_constant;
    let _e14 = blackhole_mass_scale;
    return (((((direction * _e8) * mass) * blackhole.mass) * _e14) / vec2((distance_ * distance_)));
}

fn frame_dragging(blackhole_1: BlackHole, state_1: ObjectPhaseState, mass_1: f32) -> vec2<f32> {
    let r_vec = (blackhole_1.position - state_1.position);
    let r = length(r_vec);
    let J = ((blackhole_1.spin * blackhole_1.mass) * blackhole_1.mass);
    let v_perp = vec2<f32>(-(state_1.velocity.y), state_1.velocity.x);
    let _e18 = gravitational_constant;
    return ((mass_1 * (((2f * _e18) * J) / pow(r, 3f))) * v_perp);
}

fn total_force(state_2: ObjectPhaseState, index: u32, mass_2: f32) -> vec2<f32> {
    var total_force_1: vec2<f32> = vec2<f32>();
    var bh_index_1: u32 = 0u;
    var blackhole_2: BlackHole;

    loop {
        let _e3 = bh_index_1;
        let _e5 = blackhole_count;
        if (_e3 < _e5) {
        } else {
            break;
        }
        {
            let _e8 = bh_index_1;
            let _e10 = blackholes[_e8];
            blackhole_2 = _e10;
            let _e14 = total_force_1;
            let _e15 = blackhole_2;
            let _e18 = blackhole_gravity(_e15, state_2.position, mass_2);
            total_force_1 = (_e14 + _e18);
            let _e20 = total_force_1;
            let _e21 = blackhole_2;
            let _e22 = frame_dragging(_e21, state_2, mass_2);
            total_force_1 = (_e20 + _e22);
        }
        continuing {
            let _e24 = bh_index_1;
            bh_index_1 = (_e24 + 1u);
        }
    }
    let _e32 = forces[(index * 2u)];
    let fx = bitcast<f32>(_e32);
    let _e40 = forces[((index * 2u) + 1u)];
    let fy = bitcast<f32>(_e40);
    let _e42 = total_force_1;
    total_force_1 = (_e42 + vec2<f32>(fx, fy));
    let _e45 = total_force_1;
    return _e45;
}

fn integrate_euler_symplectic(state_3: ObjectPhaseState, index_1: u32, mass_3: f32) -> ObjectPhaseState {
    var new_state: ObjectPhaseState;

    let _e1 = global_acceleration;
    let _e5 = total_force(state_3, index_1, mass_3);
    let a = (_e1 + (_e5 / vec2(mass_3)));
    new_state = state_3;
    let _e11 = new_state.velocity;
    let _e13 = dt;
    new_state.velocity = (_e11 + (a * _e13));
    let _e17 = new_state.position;
    let _e19 = new_state.velocity;
    let _e21 = dt;
    new_state.position = (_e17 + (_e19 * _e21));
    let _e24 = new_state;
    return _e24;
}

@compute @workgroup_size(64, 1, 1) 
fn integrate(@builtin(global_invocation_id) gid: vec3<u32>, @builtin(num_workgroups) nwg: vec3<u32>) {
    var f: u32;
    var state: ObjectPhaseState;
    var bh_index: u32 = 0u;
    var local: bool;

    let _e4 = thread_offset;
    let object_index = (gid.x + _e4);
    let _e7 = object_count;
    if (object_index >= _e7) {
        return;
    }
    let initial_position = positions[object_index].inner;
    let initial_velocity = velocities[object_index].inner;
    let mass_4 = masses[object_index].inner;
    let _e24 = flags[object_index].inner;
    f = _e24;
    let _e29 = velocities[object_index].inner;
    state = ObjectPhaseState(initial_position, _e29);
    let _e32 = f;
    if ((_e32 & FLAG_PHYSICALX_naga_oil_mod_XMNXW23LPNYX) != 0u) {
        let _e37 = state;
        let _e38 = integrate_euler_symplectic(_e37, object_index, mass_4);
        state = _e38;
    }
    loop {
        let _e40 = bh_index;
        let _e42 = blackhole_count;
        if (_e40 < _e42) {
            let _e44 = f;
            local = ((_e44 & FLAG_PHYSICALX_naga_oil_mod_XMNXW23LPNYX) != 0u);
        } else {
            local = false;
        }
        let _e52 = local;
        if _e52 {
        } else {
            break;
        }
        {
            let _e54 = bh_index;
            let blackhole_3 = blackholes[_e54];
            let _e59 = state.position;
            let _e63 = particle_radius;
            let distance_1 = (length((blackhole_3.position - _e59)) - (_e63 * 2f));
            let _e69 = blackhole_size_scale;
            if (distance_1 < (blackhole_3.radius * _e69)) {
                let _e72 = f;
                f = (_e72 & 4294967272u);
                state.velocity = vec2<f32>();
            }
        }
        continuing {
            let _e78 = bh_index;
            bh_index = (_e78 + 1u);
        }
    }
    let _e82 = constraints.min;
    let _e84 = state.position;
    let _e89 = particle_radius;
    let overshoot_min = (_e82 - (_e84 - (vec2<f32>(1f, 1f) * _e89)));
    let _e94 = state.position;
    let _e99 = particle_radius;
    let _e104 = constraints.max;
    let overshoot_max = ((_e94 + (vec2<f32>(1f, 1f) * _e99)) - _e104);
    if (overshoot_min.x > 0f) {
        let _e111 = state.position.x;
        state.position.x = (_e111 + overshoot_min.x);
        let _e116 = state.velocity.x;
        state.velocity.x = (_e116 * -1f);
    }
    if (overshoot_max.x > 0f) {
        let _e124 = state.position.x;
        state.position.x = (_e124 - overshoot_max.x);
        let _e129 = state.velocity.x;
        state.velocity.x = (_e129 * -1f);
    }
    if (overshoot_min.y > 0f) {
        let _e137 = state.position.y;
        state.position.y = (_e137 + overshoot_min.y);
        let _e142 = state.velocity.y;
        state.velocity.y = (_e142 * -1f);
    }
    if (overshoot_max.y > 0f) {
        let _e150 = state.position.y;
        state.position.y = (_e150 - overshoot_max.y);
        let _e155 = state.velocity.y;
        state.velocity.y = (_e155 * -1f);
    }
    let _e161 = f;
    integrated_flags[object_index].inner = _e161;
    let _e165 = state.position;
    integrated_positions[object_index] = PositionX_naga_oil_mod_XMNXW23LPNYX(_e165);
    let _e171 = state.velocity;
    integrated_velocities[object_index].inner = _e171;
    return;
}
"#;
}
